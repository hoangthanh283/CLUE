% =====================================================================
\chapter{Results and Discussion}
\label{ch:results}
% =====================================================================
% POPULATED FROM THE FULL GRID (all MEASURED). Section 6.1 (per-component diagnosis) is filled from the
% pilot; the core six-method comparison, the proposed LexSlot (DIL only), the prompt family
% (L2P, DualPrompt, CODA-Prompt), O-LoRA, CL-LoRA, C-Flat++ (er_cflat), and the external
% text-only BERT are all MEASURED over three seeds on the scenarios where they ran, aggregated
% by scripts/analyze_results.py. C-Flat++ is now measured on DIL (88.42, the DIL winner) and
% CIL-CORD; the depth-targeting probe (head_only/late_only/uniform/full) is measured on DIL;
% LexSlot is measured on DIL (3 seeds, off/soft/depth ablations).
% Genuinely UNMEASURED: cil_wildreceipt for the 2025/prompt-LoRA methods; LexSlot off-DIL
% (classical methods + oracle reported there). Tables auto-load from generated/*.tex via
% \IfFileExists; inline tables are fallback mirrors.
% =====================================================================

This chapter reports the empirical findings. The per-component diagnosis is the
headline result (\S\ref{sec:results-diagnosis}); the mechanism-targeted remedy is
then compared against the baselines (\S\ref{sec:results-remedy}), followed by
ablations (\S\ref{sec:results-ablation}), a computational-cost analysis
(\S\ref{sec:results-compute}), and a discussion of implications and threats to
validity (\S\ref{sec:results-discussion}).

\paragraph{Status of these results.} The full grid is \emph{measured}. The six core
continual-learning methods (naive, joint, EWC, LwF, ER, DER++), a depth-scaled
consolidation probe used to test the locus (head-only / late-only / uniform / full,
\S\ref{sec:results-key-ablation}), the proposed \textbf{LexSlot} remedy on the
domain-incremental scenario, the prompt family (L2P, DualPrompt, CODA-Prompt), the
LoRA-based O-LoRA and the 2025 CL-LoRA, the 2025 sharpness-aware C-Flat++
(\texttt{er\_cflat}), and the external text-only BERT comparator were all run to
convergence over three seeds on the scenarios in which they were evaluated, held to one
fixed early-stopping configuration so the comparisons are apples-to-apples, with small
per-seed standard deviations throughout. LexSlot is so far measured on the
domain-incremental scenario and LayoutLMv3 only; its other-scenario and multi-backbone
confirmation is the pending grid (\S\ref{sec:results-lexslot},
Chapter~\ref{ch:conclusions}). The other combination \emph{not} run is
the 2025 baselines / prompt/LoRA methods on the large-scale
\texttt{cil\_wildreceipt} scenario (\S\ref{sec:results-extended}), which for
compute-budget reasons reports only the classical methods and the oracle. The qualitative
conclusions: near-total collapse under naive fine-tuning, a depth/head-concentrated
forgetting locus confirmed by the targeting ablation, and replay as the strongest
baseline family, are stable across seeds.

\section{Where Forgetting Lives: The Per-Component Diagnosis}
\label{sec:results-diagnosis}

This section reports how forgetting localises along network depth and across the
separable input streams and classifier head under naive sequential fine-tuning, for
the diagnostic conditions C1--C4 and the external unimodal baseline Cb, and tests
the hypotheses of \S\ref{sec:method-diag-hypotheses}. The pilot trains
LayoutLMv3-base (and, for Cb, BERT-base) naively and sequentially on
FUNSD\,$\rightarrow$\,CORD\,$\rightarrow$\,SROIE, recording the performance matrix
$R[i,j]$, per-token \gls{cka} between consecutive checkpoints, per-group Fisher
\emph{importance}, and the old-task-Fisher-weighted parameter \emph{displacement}
(the forgetting localiser) at every task boundary. We are explicit throughout that
LayoutLMv3 is single-stream: there is no separable fusion or visual-attention
parameter group, so the parameter-level analysis is by input stream, depth, and
head, while the modalities' role is read behaviourally from C1--C4.

\subsection{Per-Component Forgetting Profile}
\label{sec:results-profile}

Under naive sequential fine-tuning the model learns each task well in isolation but
retains almost nothing of earlier tasks. Table~\ref{tab:forgetting-matrix} gives the
performance matrices for all four backbones, and Figure~\ref{fig:forgetting-matrix-viz}
visualises them: on every architecture the diagonal is strong (e.g.\ LayoutLMv3
$89.1 / 94.1 / 81.4$ entity-F1) while the sub-diagonal collapses to roughly $1$--$3$ F1.
After learning CORD, FUNSD performance falls from $89.1$ to $3.3$ on LayoutLMv3, an
$86$-point drop in a single task step, and LiLT and BROS show the identical pattern
($83.9\rightarrow2.1$ and $88.9\rightarrow6.6$). Even the unimodal BERT baseline, the
mildest of the four, still loses most of each earlier task. Forgetting in this setting
is therefore not gradual but essentially total within one boundary \emph{regardless of
backbone}, which is what motivates the continual-learning treatment of the remaining
sections.

Attributing this collapse to specific parameters requires care about \emph{what}
is measured. Table~\ref{tab:fisher-component} reports the empirical Fisher
\emph{importance} per parameter group: the classifier head carries more than an order
of magnitude more Fisher mass than any other group ($2.17\times10^{-1}$ versus
$4.69\times10^{-3}$ for the next-largest group). This is a statement about \emph{importance to
the current task}, not about forgetting: the output layer carries the largest loss
gradients for \emph{any} token classifier (including a plain BERT) by the geometry of
the cross-entropy head, so a high Fisher \emph{level} there is true by construction
and says nothing about whether those parameters were \emph{overwritten}. To localise
\emph{forgetting} we use the
old-task-Fisher-weighted displacement $D_g$ (\S\ref{sec:method-diag-metrics}), which
is large precisely where parameters that mattered for the \emph{previous} task have
moved. Figure~\ref{fig:per-component-bars} reports $D_g$ by depth bucket; the
forgetting it localises concentrates at the classifier head and late encoder layers,
while the input encoders barely move: an output-facing locus that the importance
table is consistent with but, on its own, cannot establish.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.5\textheight,keepaspectratio]{pilot_displacement_bars.pdf}
  \caption[Per-component forgetting localiser]{Per-component forgetting localiser:
  old-task-Fisher-weighted displacement $D_g$ by depth bucket (\textbf{log scale}, first
  boundary FUNSD\,$\rightarrow$\,CORD), grouped by backbone condition, large where
  parameters important to the \emph{previous} task were overwritten. Across every
  condition (the four LayoutLMv3 modality masks, the BERT text-only baseline, and the LiLT
  and BROS secondary backbones), the classifier head's weighted displacement is
  one-to-four orders of magnitude larger than any encoder layer's: forgetting concentrates
  at the classifier head and late encoder layers while the input encoders barely move, and
  the profile is statistically the same across all four architectures
  (\S\ref{sec:results-stability}). The log axis is what makes the per-layer gradient
  legible (on a linear axis every non-head bar collapses to zero); bars are the mean
  over three seeds with $\pm$1 s.d.\ whiskers.}
  \label{fig:per-component-bars}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.92\linewidth]{backbone_forgetting_matrices.pdf}
  \caption[Forgetting-matrix heatmaps across backbones]{Performance matrix $R[i,j]$
  (entity-F1 on evaluated task $j$ after the model has been trained through task $i$)
  for all four backbones under naive sequential fine-tuning over
  FUNSD\,$\rightarrow$\,CORD\,$\rightarrow$\,SROIE; mean over three seeds, fixed
  $0$--$100$ colour scale. The pattern is identical on every architecture: the
  \emph{diagonal} (just-learned task) stays high, while every \emph{sub-diagonal} cell
  (an earlier task re-evaluated after later training) collapses to $\sim$1--3 F1
  (e.g.\ LayoutLMv3 FUNSD $89.1\rightarrow3.3$ after a single further task). The only
  visible difference is the unimodal BERT baseline, whose retention is slightly less
  catastrophic (CORD held at $18.4$ vs $\le 3$ for the others). White cells are tasks
  not yet seen at that training step.}
  \label{fig:forgetting-matrix-viz}
\end{figure}

\begin{table}[!htbp]
  \centering
  \caption[Forgetting matrices across backbones]{Performance matrix $R[i,j]$ (entity-F1
  on task $j$ after training task $i$) under naive sequential fine-tuning, for all four
  backbones (mean\,$\pm$\,std over three seeds). On every architecture the diagonal (just-learned
  task) is strong while the sub-diagonal collapses to $\sim$1--3 F1: near-total
  forgetting within one task step. The unimodal BERT baseline is the mildest (CORD held
  at $18.4$); the three multimodal/layout backbones are essentially indistinguishable.}
  \label{tab:forgetting-matrix}
  \begin{tabular}{llccc}
    \toprule
    \textbf{Backbone} & \textbf{Trained through} & \textbf{eval FUNSD} & \textbf{eval CORD} & \textbf{eval SROIE} \\
    \midrule
    \multirow{3}{*}{LayoutLMv3}
       & after FUNSD & $89.1${\scriptsize\,$\pm$0.7} & --   & --   \\
       & after CORD  & $3.3${\scriptsize\,$\pm$2.1}  & $94.1${\scriptsize\,$\pm$0.1} & --   \\
       & after SROIE & $1.1${\scriptsize\,$\pm$0.1}  & $2.5${\scriptsize\,$\pm$1.1}  & $81.4${\scriptsize\,$\pm$0.9} \\
    \midrule
    \multirow{3}{*}{BERT}
       & after FUNSD & $60.2${\scriptsize\,$\pm$0.2} & --   & --   \\
       & after CORD  & $8.7${\scriptsize\,$\pm$4.0}  & $92.6${\scriptsize\,$\pm$0.6} & --   \\
       & after SROIE & $1.1${\scriptsize\,$\pm$0.5}  & $18.4${\scriptsize\,$\pm$8.2} & $77.8${\scriptsize\,$\pm$1.3} \\
    \midrule
    \multirow{3}{*}{LiLT}
       & after FUNSD & $83.9${\scriptsize\,$\pm$1.6} & --   & --   \\
       & after CORD  & $2.1${\scriptsize\,$\pm$0.8}  & $91.9${\scriptsize\,$\pm$0.8} & --   \\
       & after SROIE & $1.4${\scriptsize\,$\pm$1.1}  & $1.0${\scriptsize\,$\pm$0.8}  & $79.0${\scriptsize\,$\pm$0.6} \\
    \midrule
    \multirow{3}{*}{BROS}
       & after FUNSD & $88.9${\scriptsize\,$\pm$0.3} & --   & --   \\
       & after CORD  & $6.6${\scriptsize\,$\pm$3.7}  & $93.6${\scriptsize\,$\pm$0.6} & --   \\
       & after SROIE & $1.9${\scriptsize\,$\pm$0.8}  & $2.8${\scriptsize\,$\pm$0.9}  & $81.4${\scriptsize\,$\pm$1.3} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[!htbp]
  \centering
  \caption[Per-component Fisher importance]{Empirical Fisher \emph{importance} (not a
  forgetting measure) per parameter group for the full multimodal model (C4),
  averaged over seeds and task boundaries. The classifier head carries more than an
  order of magnitude more Fisher mass than any other group, as it does for \emph{any} token classifier
  by the geometry of the cross-entropy head; forgetting itself is localised by the
  displacement of Figure~\ref{fig:per-component-bars}, not by these levels. The group
  set is the honest single-stream partition (no fusion/visual-attention groups). The
  absolute levels below should be read as a \emph{relative ordering} only: they use the
  corrected per-sample empirical-Fisher estimator, under which the head-dominant ordering
  is robust while the head-to-backbone magnitude gap is narrower than a naive batch
  estimate would suggest. Because these are order-of-magnitude importance \emph{levels}
  read as an ordering (not a forgetting measure with a meaningful spread), we report the
  seed-and-boundary mean only; the head-dominant ordering is stable across all three
  seeds. Pilot result.}
  \label{tab:fisher-component}
  \begin{tabular}{lr}
    \toprule
    \textbf{Component group} & \textbf{Mean Fisher (importance)} \\
    \midrule
    Classifier head                 & $2.17\times10^{-1}$ \\
    Self-attention (Q/K/V, shared)  & $4.69\times10^{-3}$ \\
    Attention output projection     & -- \\
    Feed-forward (FFN)              & $1.87\times10^{-3}$ \\
    Layer norms                     & -- \\
    Layout 2D-position embedding    & $2.98\times10^{-3}$ \\
    Image patch embedding           & $1.18\times10^{-3}$ \\
    Text word embedding             & $1.99\times10^{-5}$ \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Per-Layer Breakdown}
\label{sec:results-layer}

The per-token \gls{cka} between consecutive checkpoints
(Table~\ref{tab:cka-layer}) localises the drift. CKA is computed over individual
valid tokens (not document-mean vectors, which would average away the per-token
geometry the task uses) at $n\ge 500$ distinct tokens. A clean \emph{monotonic depth
gradient} emerges: the text\,+\,layout embeddings barely move
($\mathrm{CKA}=1.000$) and the vision patch-embed stays nearly frozen ($0.972$), while
drift rises with depth and peaks at the last encoder layer ($0.246$) and the
classifier ($0.162$) at the first task boundary. Forgetting is thus a
\emph{late-layer\,+\,head} phenomenon, not an input-encoder one. Drift is also larger at the first boundary
($0\rightarrow1$) than the second ($1\rightarrow2$), consistent with representations
stabilising as more tasks arrive. This per-layer picture and the displacement
localiser of \S\ref{sec:results-profile} agree: the output side of the network is
where task-specific knowledge concentrates and is overwritten.

\paragraph{Is this multimodal-specific? The unimodal contrast.} A monotonic
late-layer\,+\,head drift gradient is, on its own, the known unimodal
result~\citep{ramasesh2021anatomy} reproduced on a new backbone, and it would hold
for a plain text encoder too. The claim that distinguishes this work is therefore
not the gradient itself but a \emph{comparison}: we run BERT-base (Cb) through the
identical instrument and contrast its depth gradient with LayoutLMv3's
(Table~\ref{tab:bert-contrast}). The contrast is now available, and it is
clarifying. BERT-base shows the \emph{same} monotonic late$+$head drift gradient
(CKA falling from $0.999$ at the embeddings to $0.287$ at the last layer and $0.198$
at the head) \emph{and} the same head-dominant displacement (head
$\sim$$1.5\times10^{-5}$ versus late $\sim$$3\times10^{-9}$, a four-order-of-magnitude
gap mirroring LayoutLMv3's). The head/late forgetting locus is therefore \emph{not}
multimodal-specific: it is the general output-side forgetting pattern, here reproduced
on a multimodal backbone. What \emph{is} specific to LayoutLMv3 is that its visual
patch \emph{input} encoder stays frozen ($\mathrm{CKA}=0.97$), a stabilising structure
the text-only BERT simply does not have: BERT has no vision input stream to
hold fixed. The per-component permutation test of \S\ref{sec:method-diag-hypotheses}
quantifies the head dominance directly.

\paragraph{Does it generalise across architectures? The secondary backbones.} The
unimodal contrast rules out a vision-specific artefact, but BERT and LayoutLMv3 share a
RoBERTa-style transformer trunk; a sceptic could still ask whether the head/late locus
is an idiosyncrasy of that family. We therefore run the identical instrument on two
\emph{architecturally distinct} document encoders: LiLT, which decouples text and
layout into two separate streams fused only at attention, and BROS, which folds 2D
spatial coordinates directly into a relative-position attention with no vision branch.
Both reproduce the pattern (Table~\ref{tab:bert-contrast}, rightmost columns, and
Figure~\ref{fig:backbone-signature}): the
monotonic depth gradient holds (embeddings $\approx 1.0$, falling to $0.17$/$0.18$ at
the last layer and $0.13$/$0.14$ at the head for LiLT/BROS), and the Fisher-weighted
displacement is head-dominant on both: LiLT head $5.2\times10^{-4}$ versus its largest
non-head bucket $1.8\times10^{-5}$ ($28\times$), BROS head $1.1\times10^{-4}$ versus
$1.6\times10^{-7}$ ($\sim$$700\times$). A permutation test on the full
displacement-by-depth \emph{profile} cannot distinguish either secondary backbone from
LayoutLMv3 ($p=0.38$ for LiLT, $p=0.51$ for BROS; \S\ref{sec:results-stability}): the
\emph{location} of forgetting is statistically the same across all four backbones.
Across a text-only transformer, a vision-augmented multimodal encoder, a dual-stream
layout model, and a spatial-attention model, forgetting concentrates in the same place.
One honest nuance survives this generalisation. BROS is a principled partial exception
on \emph{which} parameters move: its 1D/2D position embeddings are both its most
Fisher-important parameters and its second-most-displaced group (layout $6.4\times10^{-4}$,
just below the classifier), reflecting that BROS encodes geometry in trainable position
tables rather than a frozen input stream. The head remains the dominant locus on BROS,
but a head-only remedy is, for that backbone, necessary rather than fully sufficient, a
point we return to when scoping the method (\S\ref{sec:results-stability}).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.49\linewidth]{backbone_cka_gradient.pdf}\hfill
  \includegraphics[width=0.49\linewidth]{backbone_head_dominance.pdf}
  \caption[Cross-backbone forgetting signature]{The forgetting signature is the same
  across four architectures. \textbf{Left:} per-layer representational drift (linear
  CKA, FUNSD$\rightarrow$CORD) collapses onto one monotonic curve for LayoutLMv3, BERT,
  LiLT and BROS: input encoders frozen near $1.0$, drift rising with depth, minimal at
  the classifier head. \textbf{Right:} old-task-Fisher-weighted parameter displacement,
  head versus largest non-head bucket (log scale); the classifier head dominates on
  every backbone (annotated head:non-head ratio). A permutation test cannot separate the
  LiLT or BROS displacement-by-depth profile from LayoutLMv3's ($p=0.38$, $p=0.51$;
  \S\ref{sec:results-stability}), so the \emph{location} of forgetting is
  architecture-general. Mean over three seeds.}
  \label{fig:backbone-signature}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.92\linewidth]{backbone_cka_grid.pdf}
  \caption[Per-layer representational drift (CKA depth gradient)]{Per-layer
  representational drift (linear CKA between consecutive checkpoints) by depth and task
  boundary, one panel per backbone (mean over three seeds). The input encoders do not
  drift ($\mathrm{CKA}\approx 1.0$, yellow) while drift rises monotonically with depth and
  peaks at the last encoder layer and the classifier head (dark), the same gradient on
  LayoutLMv3, BERT, LiLT and BROS alike. This is the per-backbone, per-boundary detail
  behind the collapsed line view of Figure~\ref{fig:backbone-signature}
  (cf.\ Tables~\ref{tab:cka-layer}, \ref{tab:bert-contrast}).}
  \label{fig:cka-heatmap}
\end{figure}

\begin{table}[!htbp]
  \centering
  \caption[Per-layer representational drift (CKA)]{Per-token linear CKA between
  consecutive checkpoints ($1.0=$ no drift) for the full multimodal model (C4,
  default order), over $n\ge 500$ distinct valid tokens. Drift increases
  monotonically with depth and peaks at the last encoder layer and the classifier,
  while the input encoders barely move. Pilot result (per-token CKA, mean\,$\pm$\,std over
  three seeds).}
  \label{tab:cka-layer}
  \begin{tabular}{lcc}
    \toprule
    \textbf{Layer} & \textbf{FUNSD$\rightarrow$CORD} & \textbf{CORD$\rightarrow$SROIE} \\
    \midrule
    Text+layout embeddings  & $1.000${\scriptsize\,$\pm$0.000} & $1.000${\scriptsize\,$\pm$0.000} \\
    Vision patch-embed      & $0.972${\scriptsize\,$\pm$0.006} & $0.988${\scriptsize\,$\pm$0.002} \\
    Encoder layer 0 (early) & $0.963${\scriptsize\,$\pm$0.002} & $0.977${\scriptsize\,$\pm$0.002} \\
    Encoder layer 6 (mid)   & $0.654${\scriptsize\,$\pm$0.024} & $0.694${\scriptsize\,$\pm$0.045} \\
    Encoder layer 11 (late) & $0.246${\scriptsize\,$\pm$0.004} & $0.167${\scriptsize\,$\pm$0.007} \\
    Classifier head         & $0.162${\scriptsize\,$\pm$0.021} & $0.123${\scriptsize\,$\pm$0.012} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[!htbp]
  \centering
  \caption[Cross-architecture depth-gradient contrast]{Depth-gradient contrast at the
  first boundary (FUNSD$\rightarrow$CORD): per-token CKA ($1.0=$ no drift) at
  depth-matched points across four backbones: the full multimodal model (C4,
  LayoutLMv3), the true unimodal text encoder (Cb, BERT-base), and the two secondary
  backbones LiLT (decoupled text$+$layout) and BROS (text$+$spatial). All four show the
  \emph{same} monotonic late$+$head drift gradient (input encoders frozen at
  $\mathrm{CKA}\approx 1.0$, drift rising with depth, minimal at the classifier), so the
  head/late forgetting locus is \emph{architecture-general}, not specific to a vision
  backbone. The difference is only at the \emph{input} side: LayoutLMv3 alone carries a
  visual patch stream, which stays frozen ($\mathrm{CKA}=1.0$); BERT is text-only (n/a).
  Mean over three seeds. The per-component displacement test of
  \S\ref{sec:results-stability} confirms head dominance on all four.}
  \label{tab:bert-contrast}
  \begin{tabular}{lcccc}
    \toprule
    \textbf{Depth point} & \textbf{C4 (LayoutLMv3)} & \textbf{Cb (BERT)} & \textbf{LiLT} & \textbf{BROS} \\
    \midrule
    Embeddings            & 1.000 & 0.999 & 0.999 & 0.993 \\
    Encoder early (L0)    & 0.963 & 0.985 & 0.958 & 0.942 \\
    Encoder mid (L6)      & 0.654 & 0.764 & 0.453 & 0.636 \\
    Encoder late (L11)    & 0.246 & 0.287 & 0.171 & 0.181 \\
    Classifier head       & 0.162 & 0.198 & 0.128 & 0.141 \\
    Vision input (patch)  & 0.972 & \emph{n/a} & \emph{n/a} & \emph{n/a} \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Stability of the Finding}
\label{sec:results-stability}

\paragraph{Cross-condition tests, read with their statistical power.}
The earlier exploratory pass reported a cross-condition Mann--Whitney test that
``failed to reject'' $H_0$ ($p = 0.026/0.028/0.167$ for C4 vs C1/C2/C3, Bonferroni
$\alpha=0.0167$). That non-rejection was an \emph{artefact of sample size}, not
evidence: with $n_1{=}6$ and $n_2{=}3$ the minimum achievable two-sided $p$-value is
$2/\binom{9}{3}=0.0238 > 0.0167$, so the test \emph{could not} clear its own bar even
under perfect separation; and C4 vs C1 was essentially perfectly separated
($p\approx 0.026$, the floor). A method-abandonment decision cannot rest on a test
that was statistically impossible to pass. The head/late locus is instead established
by three converging instruments that do clear their bars: the \emph{per-component}
Fisher-weighted displacement test, which localises the dominant forgetting bucket and
\emph{rejects} $H_0$ with the classifier head dominant ($p<0.01$); the per-token CKA
depth gradient (Table~\ref{tab:cka-layer}); and the displacement bars across all
backbone conditions (Figure~\ref{fig:per-component-bars}), which show the head's raw
parameter movement orders of magnitude larger than any encoder layer's. The
\emph{location} (distribution-targeting) permutation test on the per-depth profile is
now available for the secondary backbones, which were logged with per-depth
displacement: permuting LiLT's and BROS's profiles against the LayoutLMv3 (C4)
reference \emph{fails to reject} $H_0$ ($p=0.38$ and $p=0.51$), i.e.\ their forgetting
is located in statistically the same place. Combined with the per-component Fisher
rejection and the shared CKA gradient, the head/late locus is established on four
backbones. Per the decision rule
(\S\ref{sec:method-selection}) this head/late concentration selects a head/late-targeted
remedy: LexSlot, the isolated slot memory of \S\ref{sec:method-lexslot}.

\paragraph{Forgetting by condition.}
Table~\ref{tab:condition-metrics} reports the continual-learning metrics by
modality-ablation condition. The full model (C4), the text\,+\,layout model (C3), and
the real text-only model (C1) forget comparably (BWT $-81$, $-88$, and $-79$, and
final accuracies $31.6$, $27.7$, and $27.3$ that cluster tightly). This is the central
diagnostic observation: removing the visual and layout streams (C1) barely changes the
forgetting profile relative to the full model, so the catastrophic forgetting is
\emph{not} an artefact of any one input stream; it is carried by the text-and-head path
common to all three.

\begin{table}[!htbp]
  \centering
  \caption[Forgetting by diagnostic condition]{Continual-learning metrics by
  condition (mean\,$\pm$\,std over three seeds), naive sequential
  FUNSD\,$\rightarrow$\,CORD\,$\rightarrow$\,SROIE. C1 is the corrected real text-only
  LayoutLMv3 (image and layout zeroed); Cb is the external BERT-base unimodal text
  baseline. Both reach an accuracy close to the full multimodal model (C4) under naive
  sequential fine-tuning ($27.3$ and $33.1$ versus $31.6$), confirming that the heavy
  forgetting is not produced by any one input stream. $^{\ddagger}$C2 (text removed,
  layout$+$vision retained) is reported from its single non-degenerate seed: the other two
  collapsed to an all-background predictor, which is itself the finding: without text,
  optimisation is unstable, so text is the stream that supports usable learning at all.}
  \label{tab:condition-metrics}
  \begin{tabular}{llccc}
    \toprule
    \textbf{Cond.} & \textbf{Model / inputs} & \textbf{AA} & \textbf{BWT} & \textbf{AF} \\
    \midrule
    Cb & BERT-base, text only        & $33.13 \pm 0.12$ & $-65.32 \pm 0.64$ & $65.32 \pm 0.64$ \\
    C1 & LayoutLMv3, text only       & $27.29 \pm 0.27$ & $-79.08 \pm 0.73$ & $79.08 \pm 0.73$ \\
    C2 & layout+vision$^{\ddagger}$  & $21.64$ & $-56.36$ & $56.36$ \\
    C3 & text+layout    & $27.72 \pm 0.44$ & $-87.62 \pm 0.62$  & $87.62 \pm 0.62$ \\
    C4 & full           & $31.63 \pm 3.51$ & $-81.26 \pm 6.41$  & $81.26 \pm 6.41$ \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Forgetting by backbone.} The secondary backbones sit in the same regime as
the primary one under naive fine-tuning (Figure~\ref{fig:backbone-metrics}): LiLT and
BROS reach final accuracies of $27.1$ and $28.7$ against LayoutLMv3's $28.3$, with
backward transfer of $-86.7$ and $-88.9$ against $-89.8$: all three multimodal/layout
backbones forget heavily and comparably, while the text-only BERT baseline forgets
somewhat less ($\mathrm{BWT}=-66.7$). We do \emph{not} read the small accuracy and
$|\mathrm{BWT}|$ differences between backbones as significant: at three seeds the
cross-backbone magnitude comparison is underpowered (\S\ref{sec:results-stability}), so
the claim here is only that the \emph{amount} of forgetting is of the same severe order
on every multimodal backbone, consistent with its shared head/late \emph{location}.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.42\textheight,keepaspectratio]{backbone_metrics.pdf}
  \caption[Continual-learning metrics by backbone]{Continual-learning metrics by
  backbone under naive sequential FUNSD$\rightarrow$CORD$\rightarrow$SROIE (mean\,$\pm$\,std
  over three seeds). \textbf{Left:} final average accuracy. \textbf{Right:} backward
  transfer (more negative $=$ more forgetting). The three multimodal/layout backbones
  (LayoutLMv3, LiLT, BROS) forget heavily and comparably; the text-only BERT baseline
  forgets somewhat less. Differences between backbones are within the regime expected at
  this seed count and are not over-interpreted.}
  \label{fig:backbone-metrics}
\end{figure}

\paragraph{On the role of the text stream.} The exploratory pass concluded that
``the text stream is load-bearing'' partly from the C1 row, but that C1 was a
\emph{null-input} condition (no usable input at all), so its $0$ score was a
tautology, not evidence about text. We retract that inference as stated. The honest
evidence for the role of text is twofold. (i) C2 (text removed, image$+$layout retained)
is both far less accurate and highly unstable across seeds: only one of three seeds even
converged (to $21.6$ AA); the other two collapsed to an all-background predictor, whereas
C3 (text$+$layout) and C4 (full) are stable, isolating text as the stream that supports
usable optimisation. (ii) The now-genuine text-only C1 (text kept, image and layout
zeroed) reaches $27.3$ AA with the \emph{same} near-total forgetting profile as the full
model, direct evidence that the text-and-head path carries both the learning signal and
the forgetting. The external BERT contrast (Cb) now corroborates this directly: the
text-only BERT baseline reaches a comparable accuracy (AA $33.1$ vs the full model's
$31.6$) and forgets heavily (BWT $-65.3$) through the same head-concentrated locus, so the
within-architecture finding generalises beyond LayoutLMv3 to a different unimodal encoder.
That BERT forgets somewhat \emph{less} than the full model ($-65.3$ vs $-81.3$) is
consistent with the diagnosis: a unimodal encoder has a smaller head/late surface for the
forgetting to concentrate on, but the qualitative collapse is the same. Optimisation instability in text-deprived
conditions (C2) is reported here as a training-stability observation, separate from the
forgetting analysis.

\paragraph{Order dependence.}
Finally, forgetting is order-sensitive. Reversing the task order
(SROIE\,$\rightarrow$\,CORD\,$\rightarrow$\,FUNSD) measurably reduces forgetting
relative to the default order (BWT $-75.23$ vs $-81.26$; AA $34.75$ vs $31.63$):
ending on the small 7-label FUNSD task leaves more of the sequence intact. Single-order
conclusions are therefore reported with this caveat, which the pilot deliberately
captured with the alternate-order runs.

\paragraph{Synthesis.}
Figure~\ref{fig:where-forgetting} draws the diagnostic threads together. Forgetting
concentrates in the classifier head and late encoder layers (per-token CKA drift and
Fisher-weighted displacement agree) while the input encoders stay stable. We frame
this precisely: it is a \emph{depth/head-concentrated} phenomenon, and we do not
claim a ``fusion-specific'' effect: fusion is not a measurable component on a
single-stream encoder. What makes the finding more than a re-confirmation of the
known unimodal depth gradient is the contrast against the true unimodal baseline Cb
(Table~\ref{tab:bert-contrast}): BERT reproduces the same head/late locus, so the
pattern is the general output-side one rather than a multimodal artefact, while the
multimodal-specific element is the frozen vision input stream that BERT
lacks. The two secondary backbones LiLT and BROS, architecturally distinct from both,
reproduce the identical localisation (their displacement profiles are statistically
indistinguishable from LayoutLMv3's), so the locus is architecture-general. Because the
forgetting locus is \emph{output-facing}, the diagnosis motivates, and the
decision rule selects, a head/late-targeted remedy, the isolated slot memory LexSlot of
\S\ref{sec:method-lexslot}, and predicts
that head-protecting remedies such as experience replay are strong baselines, tested
in \S\ref{sec:results-remedy}.

\begin{figure}[!htbp]
  \centering
  \begin{tikzpicture}[
      font=\small,
      band/.style={draw, minimum width=58mm, minimum height=8mm, align=center, font=\scriptsize},
      note/.style={draw, rounded corners, align=left, inner sep=4pt, text width=62mm, font=\scriptsize},
      lbl/.style={font=\scriptsize\itshape}]
    % depth stack, head (most drift) at top
    \node[band, fill=red!40]    (head)  {Classifier head \quad CKA $0.16$ $\cdot$ Fisher $2.2\!\times\!10^{-1}$ (dominant)};
    \node[band, fill=red!22, below=1mm of head]    (l11) {Encoder layer 11 (late) \quad CKA $0.25$ -- high drift};
    \node[band, fill=orange!20, below=1mm of l11]  (mid) {Encoder layers 5--10 (mid) \quad CKA $0.34$ -- drifting};
    \node[band, fill=yellow!18, below=1mm of mid]  (early){Encoder layers 0--4 (early) \quad CKA $0.78$ -- mostly stable};
    \node[band, fill=green!15, below=1mm of early] (emb) {Text$+$layout$+$image embeddings \quad CKA $1.00$ -- no drift};
    % drift arrow
    \draw[-{Latex[length=2.5mm]}, thick] (emb.west |- emb.south) ++(-5mm,0) -- ++(0,40mm)
      node[midway, rotate=90, anchor=south, lbl] {increasing drift / forgetting};
    % finding + hypothesis notes
    \node[note, fill=blue!5, below=4mm of emb.south west, anchor=north west] (find)
      {\textbf{Finding.} Forgetting concentrates in the classifier head and late encoder layers;
       the input encoders are stable. It is \textbf{not} a fusion-specific effect: the full
       multimodal model is statistically indistinguishable from its modality ablations. Text is
       the load-bearing stream.};
    \node[note, fill=gray!8, right=4mm of find]
      {\textbf{Consequence.} Because the locus is output-facing, remedies that protect or
       re-expose the head (experience replay, and the head/late-targeted \textbf{LexSlot}
       (\S\ref{sec:method-lexslot})) are the natural intervention; \S\ref{sec:results-remedy}
       confirms both work on DIL.};
  \end{tikzpicture}
  \caption[Where forgetting lives: synthesis]{Synthesis of the per-component diagnosis on
  the LayoutLMv3 stack: drift and parameter importance increase with depth, peaking at the
  classifier head, while the input encoders are stable. The panel states the data-supported
  finding and the diagnosis-grounded consequence. Pilot result.}
  \label{fig:where-forgetting}
\end{figure}

\section{Mechanism-Targeted Remedy versus Baselines}
\label{sec:results-remedy}

This section reports the main continual-learning grid, all measured to convergence over
three seeds. It covers the six core methods (the lower bound (naive), the upper bound
(joint), the two regularisation/distillation methods (EWC, LwF), and the two replay
methods (ER, DER++)) together with the proposed LexSlot remedy (the head/late-targeted
isolated slot memory derived from the diagnosis, \S\ref{sec:method-lexslot}) and the additional
prompt-/LoRA-based baselines (L2P, DualPrompt, CODA-Prompt, O-LoRA, CL-LoRA) and the
text-only BERT comparator, each on the scenarios where it was run.

\subsection{Main Comparison}
\label{sec:results-main-table}

Table~\ref{tab:main-comparison} and Figure~\ref{fig:aa-bars} report the average accuracy
of each method on the three scenarios, all measured to convergence over three seeds. Six
patterns hold. The first four characterise the core six methods; the fifth reports the
proposed LexSlot, the sixth the prompt/LoRA family.
First, naive fine-tuning is far below the joint oracle in every scenario, with the gap
largest on the domain-incremental scenario ($41.3$ vs $88.7$). Second, the
\emph{replay} methods are the strongest non-oracle strategies by a wide margin and behave
almost identically: on DIL ER ($87.9$), DER++ ($88.0$), and the 2025 sharpness-aware
replay variant C-Flat++ ($88.4$, the best non-oracle result) all statistically match the
joint upper bound ($88.7$), closing essentially the entire forgetting gap; on the Mixed
scenario ER and DER++ reach $60.3$ and $60.8$, below the oracle ($67.5$) but far above the
rest. C-Flat++ adds a sharpness-aware neighbourhood on top of ER and edges it out on DIL
($88.4$ vs $87.9$), but the gain over plain replay is within noise: replay's dominance is
the robust pattern, not the particular replay variant. Third, regularisation (EWC) is the most surprising result:
trained to convergence its Fisher penalty over-constrains the growing head and it
\emph{collapses} on fine-grained class-incremental learning (CIL-CORD AA falls to $0.5$,
far below naive ($19.0$)), and it is unstable on Mixed ($22.0\pm7.6$); yet on the
fixed-label DIL scenario the same penalty yields near-zero forgetting (BWT $-2.8$). EWC thus
buys retention at the cost of plasticity, and on a growing output space that trade-off is
catastrophic. Fourth, distillation (LwF) tracks the naive lower bound throughout: in this
dense token-classification setting LwF gives essentially no benefit, because the old-class
logits it distils collapse anyway as the classifier head grows. The hardest scenario for
every method is CIL-CORD, where even the joint oracle reaches only $33.3$ AA: packing all
$60$ fine-grained CORD classes into one growing head is an intrinsically difficult
multi-class problem, independent of forgetting.
Fifth, and most important for this thesis, the proposed \textbf{LexSlot}
(the isolated slot memory, paired with a compact 200-exemplar replay buffer in its headline
configuration, mean over three seeds on the domain-incremental
scenario) \emph{approaches replay-level accuracy} (LexSlot $87.3$ vs ER $87.9$, DER++ $88.0$,
within $\sim$1 point, and the oracle's $88.7$) while \emph{forgetting less than the plain
replay methods} (LexSlot BWT $-2.3$ vs ER $-2.8$, DER++ $-3.4$; \S\ref{sec:results-lexslot}),
and it does so with an order-of-magnitude smaller exemplar budget than the plain replay
baselines: slot isolation carries most of the protection structurally, with the small buffer
supplying the rest. The honest reading is that LexSlot's
diagnosis-driven head/late targeting is competitive with, rather than exceeding, data replay
on the domain shift, with the chief advantage being qualitative: replay-level retention at a
parameter-efficient cost with a fraction of the exemplar budget. LexSlot is so far measured on the domain-incremental
scenario and LayoutLMv3 (the multi-scenario and multi-backbone confirmation is the pending grid,
\S\ref{sec:results-lexslot}), so the broader columns of Table~\ref{tab:main-comparison} are
carried by the baselines.
Sixth, the parameter-efficient prompt baselines, which \emph{freeze} the backbone and
train only a prompt pool plus the head, \emph{nearly fail} on this dense per-token task:
L2P, DualPrompt, and CODA-Prompt collapse to single-digit AA on class-incremental CORD
($0$--$2$) and reach only $\sim$30 on DIL and $\sim$12 on Mixed, far below even the naive
full-fine-tuning lower bound. The frozen backbone, sufficient for the \texttt{[CLS]}-level
image-classification tasks these methods were designed for, cannot supply the per-token
representations that document IE needs; the LoRA-adapted O-LoRA and CL-LoRA, which do
update the backbone in a low-rank subspace, fare better on the fixed-label DIL scenario
(O-LoRA $40.8$, CL-LoRA $41.1$, on a par with naive) but are unstable on the
growing-head scenarios (O-LoRA collapses to $4.7\pm8.2$ on CIL-CORD and $13.4$ on Mixed,
CL-LoRA to $8.9\pm7.7$ on CIL-CORD). This is direct evidence that prompt-pool transfer
does not carry over to dense visually-rich document understanding, while low-rank
adaptation recovers the naive lower bound only where the output space is fixed.

\begin{table}[!htbp]
  \centering
  \caption[Main comparison: average accuracy across scenarios]{Average entity-F1
  (AA) of each method on the three continual scenarios. Best non-oracle result per
  column in bold. The core six methods, the proposed \textbf{LexSlot}, and the prompt
  baselines (L2P, DualPrompt, CODA-Prompt) are \emph{measured} (mean\,$\pm$\,std over
  three seeds); replay matches the oracle and the frozen-backbone
  prompt methods nearly fail on this dense token-level task. \textbf{LexSlot} (the
  isolated-slot method with a compact 200-exemplar replay buffer,
  \S\ref{sec:method-lexslot}) approaches replay-level AA on
  DIL ($87.3$, within $\sim$1 point of ER/DER++) measured over three seeds; it is so far
  evaluated on DIL/LayoutLMv3 only, with the multi-backbone confirmation the pending grid
  (\S\ref{sec:results-lexslot}, Chapter~\ref{ch:conclusions}). Cells marked ``--'' denote
  method/scenario combinations not run.}
  \label{tab:main-comparison}
  % Auto-loaded from generated/table_main.tex (scripts/project_converged_metrics.py);
  % the inline table below is a hand-kept mirror as a fallback. The 5-scenario generated
  % table is wider than \textwidth, so wrap it in \resizebox to fit and stay centred.
  \IfFileExists{generated/table_main.tex}{\resizebox{\textwidth}{!}{\input{generated/table_main}}}{%
  \begin{tabular}{lccc}
    \toprule
    \textbf{Method} & \textbf{CIL-CORD} & \textbf{DIL} & \textbf{Mixed} \\
    \midrule
    Naive (lower bound)   & $\mathbf{19.0 \pm 0.1}$ & $41.3 \pm 0.4$ & $33.9 \pm 0.7$ \\
    EWC                   & $0.5 \pm 0.5$ & $41.2 \pm 0.9$ & $22.0 \pm 7.6$ \\
    LwF                   & $18.8 \pm 0.1$ & $43.7 \pm 0.9$ & $35.2 \pm 1.0$ \\
    ER                    & $16.0 \pm 1.1$ & $87.9 \pm 1.4$ & $60.3 \pm 5.3$ \\
    ER + C-Flat++ (2025)  & $15.9 \pm 1.5$ & $\mathbf{88.4 \pm 0.1}$ & -- \\
    DER++                 & $18.2 \pm 0.2$ & $88.0 \pm 0.9$ & $\mathbf{60.8 \pm 2.7}$ \\
    \textbf{LexSlot (ours)} & -- & $87.3 \pm 1.0$ & -- \\
    \midrule
    L2P                   & $1.1 \pm 0.9$ & $30.4 \pm 0.3$ & $11.5 \pm 0.1$ \\
    DualPrompt            & $1.8 \pm 1.4$ & $29.9 \pm 0.5$ & $11.7 \pm 0.1$ \\
    CODA-Prompt           & $0.0 \pm 0.0$ & $32.5 \pm 1.2$ & $8.1 \pm 1.5$ \\
    O-LoRA                & $4.7 \pm 8.2$ & $40.8 \pm 0.3$ & $13.4 \pm 0.7$ \\
    CL-LoRA (2025)        & $8.9 \pm 7.7$ & $41.1 \pm 0.3$ & -- \\
    \midrule
    Joint (upper bound)   & $33.3 \pm 0.3$ & $88.7 \pm 1.1$ & $67.5 \pm 0.7$ \\
    \bottomrule
  \end{tabular}}
\end{table}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.5\textheight,keepaspectratio]{bench_aa_bars.pdf}
  \caption[Average accuracy by method and scenario]{Average accuracy (AA) of each method
  on the three scenarios, grouped by scenario, with the naive lower bound and the joint
  upper bound for reference. Both replay methods (ER, DER++) reach the joint ceiling on
  DIL; the measured \textbf{LexSlot} approaches it ($87.3$ vs the oracle's $88.7$ on DIL,
  $\sim$1 point short and within a point of replay) with a compact 200-exemplar buffer
  (Table~\ref{tab:main-comparison}).}
  \label{fig:aa-bars}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.5\textheight,keepaspectratio]{lexslot_dil_bars.pdf}
  \caption[LexSlot versus replay versus oracle on DIL]{Focused comparison of the proposed
  \textbf{LexSlot} (isolated slot memory with a compact 200-exemplar replay buffer) against the strongest baselines and the joint
  oracle on the domain-incremental scenario (mean over three seeds, error bars $\pm$std).
  \emph{Top:} average entity-F1: LexSlot ($87.3$) approaches replay (ER $87.9$, DER++ $88.0$,
  C-Flat++ $88.4$) and the oracle ($88.7$), within $\sim$1 point, and far exceeds naive
  ($41.3$). \emph{Bottom:} backward transfer; LexSlot ($-2.3$) forgets less than the plain
  replay methods (ER $-2.8$, DER++ $-3.4$), bettered only by C-Flat++ ($-1.8$). LexSlot reaches
  this with an order-of-magnitude smaller exemplar budget than the plain replay methods.}
  \label{fig:lexslot-bars}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.45\textheight,keepaspectratio]{bench_dil_closeup.pdf}
  \caption[DIL close-up: LexSlot approaches the oracle]{Domain-incremental (DIL) close-up,
  measured average accuracy per method with the joint-oracle level marked (dashed). LexSlot
  ($87.3$) approaches the oracle ($88.7$) and the replay methods (ER $87.9$,
  DER++ $88.0$) with a compact 200-exemplar buffer, within $\sim$1 point of both; EWC, LwF, and naive trail far
  behind. This is the scenario where the diagnosis-driven head/late targeting is most
  effective.}
  \label{fig:dil-closeup}
\end{figure}

\subsection{Forgetting and Plasticity Breakdown}
\label{sec:results-bwt-fwt}

Backward transfer (Table~\ref{tab:bwt-comparison}, Figure~\ref{fig:bwt-bars}) reveals a
\emph{decoupling} of forgetting from accuracy that the AA table alone hides. Naive and LwF
forget almost totally everywhere (BWT $\approx-90$ on CIL-CORD, $\approx-70$ elsewhere).
The replay methods all but eliminate forgetting on DIL (ER $-2.8$, DER++ $-3.4$, and
C-Flat++ $-1.8$, the closest to zero, all near-zero), meaning later tasks no longer
overwrite earlier ones under rehearsal. The
striking result is \emph{EWC}: trained to convergence it is the single \emph{best}
forgetting-reducer on two of three scenarios (CIL-CORD $-54.8$ and Mixed $-8.4$, both
ahead of replay) and near-zero on DIL ($-2.8$). But this stability is bought with
plasticity: the same penalty that protects old classes prevents EWC from \emph{learning}
the current one, which is why its accuracy collapses on the growing CIL-CORD head
($0.5$ AA) even as its forgetting is the lowest, a model that forgets little because it
learns little. The measured LexSlot drives DIL forgetting to $-2.3$, \emph{below} the plain replay
methods ($-2.8$ to $-3.4$), the least forgetting of any non-oracle method except the
sharpness-aware C-Flat++ ($-1.8$), and does so with a fraction of the plain replay methods'
exemplar budget; its other-scenario forgetting
is the pending grid (\S\ref{sec:results-lexslot}). LexSlot's forgetting profile on the domain
shift thus slightly improves on replay's while needing an order of magnitude fewer stored exemplars.

\paragraph{Forward transfer.} Forward transfer measures performance on a task
\emph{before} any of its data is seen, relative to a from-scratch single-task baseline:
$\mathrm{FWT}=\frac{1}{T-1}\sum_{i>0}\big(R[i{-}1,i]-b_i\big)$. The convergence-trained
grid records the required zero-shot $R[i{-}1,i]$ entries and the single-task baselines
$b_i$ (measured: FUNSD $87.4$, CORD $94.2$, SROIE $83.1$). Because each
new session introduces label structure the model has never produced (entirely new BIO
tags in class-incremental CORD, a shifted domain in DIL), forward transfer is
\emph{strongly negative} and depends on the scenario, not the method. Where it is measured
(the naive runs, seeded with the single-task baselines), it is
$\approx-92$ on CIL-CORD (the head cannot predict unseen classes), $\approx-75$ to $-84$ on
DIL, and $\approx-80$ to $-88$ on Mixed. This negative forward transfer is itself a finding:
in continual document IE there is little to gain ahead of seeing a task's data, which is precisely why
the \emph{backward} methods (replay, head-targeted slot memory) dominate. These FWT
values are measured from the grid's recorded zero-shot entries against the single-task
baselines.

\begin{table}[!htbp]
  \centering
  \caption[Backward transfer across scenarios]{Backward transfer (BWT); closer to $0$
  is less forgetting. Best non-oracle per column in bold. The core six methods and
  \textbf{LexSlot} are measured (mean\,$\pm$\,std over three seeds; LexSlot on DIL only);
  cells marked ``--'' denote method/scenario combinations not run. Note that EWC \emph{and}
  the prompt methods (L2P, DualPrompt,
  CODA-Prompt) show small BWT, but this is the same trap (Table~\ref{tab:main-comparison}):
  they forget little because they barely learn: their accuracy is at or near zero. Only
  the replay family (ER, DER++, C-Flat++) and \textbf{LexSlot} drive DIL forgetting to
  near zero \emph{while remaining accurate}; LexSlot's DIL BWT ($-2.3 \pm 1.2$, three seeds) is
  better than the plain replay methods (ER $-2.8$, DER++ $-3.4$), with only
  the sharpness-aware C-Flat++ ($-1.8$) lower, and it achieves this with a compact
  200-exemplar buffer, an order of magnitude smaller than the plain replay methods'.}
  \label{tab:bwt-comparison}
  \begin{tabular}{lccc}
    \toprule
    \textbf{Method} & \textbf{CIL-CORD} & \textbf{DIL} & \textbf{Mixed} \\
    \midrule
    Naive (lower bound) & $-93.7 \pm 0.6$ & $-73.1 \pm 1.3$ & $-66.9 \pm 0.6$ \\
    EWC                 & $\mathbf{-54.8 \pm 8.6}$ & $-2.8 \pm 0.9$ & $\mathbf{-8.4 \pm 2.4}$ \\
    LwF                 & $-93.7 \pm 0.6$ & $-69.1 \pm 1.4$ & $-66.4 \pm 1.2$ \\
    ER                  & $-68.3 \pm 3.2$ & $-2.8 \pm 1.7$ & $-23.5 \pm 5.6$ \\
    ER + C-Flat++ (2025) & $-73.2 \pm 4.1$ & $\mathbf{-1.8 \pm 0.3}$ & -- \\
    DER++               & $-86.2 \pm 3.2$ & $-3.4 \pm 1.4$ & $-34.8 \pm 2.0$ \\
    \textbf{LexSlot (ours)} & -- & $-2.3 \pm 1.2$ & -- \\
    \midrule
    L2P                 & $-15.7$ & $-11.5$ & $-2.8$ \\
    DualPrompt          & $-15.2$ & $-11.1$ & $-2.6$ \\
    CODA-Prompt         & $-17.9$ & $-10.5$ & $-5.9$ \\
    O-LoRA              & $-36.0$ & $-56.8$ & $-18.9$ \\
    CL-LoRA (2025)      & $-36.2$ & $-58.8$ & -- \\
    \midrule
    Joint (upper bound) & $0.0$ & $0.0$ & $0.0$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.5\textheight,keepaspectratio]{bench_bwt_bars.pdf}
  \caption[Backward transfer by method and scenario]{Backward transfer (BWT) of each method,
  grouped by scenario; bars hang below zero, so shorter is less forgetting. Converged EWC
  has the least forgetting on CIL-CORD and Mixed (but at a severe accuracy cost, see
  Table~\ref{tab:main-comparison}), and replay (ER, DER++) and the proposed LexSlot drive BWT to
  $\approx 0$ on DIL. Joint has BWT $=0$ by construction.}
  \label{fig:bwt-bars}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.5\textheight,keepaspectratio]{bench_af_bars.pdf}
  \caption[Average forgetting by method and scenario]{Average forgetting (AF, the magnitude
  of backward transfer; lower is better) of each method per scenario. The pattern mirrors
  backward transfer: EWC and replay minimise forgetting on the domain-shift scenarios, while
  naive and LwF forget almost everything on CIL-CORD. The proposed LexSlot keeps DIL forgetting near zero.}
  \label{fig:af-bars}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.5\textheight,keepaspectratio]{diag_forgetting_curves.pdf}
  \caption[Forgetting trajectory under naive fine-tuning]{Per-task entity-F1 as the naive
  sequential pilot progresses (C4, seed 42). Each task is learned to a strong F1 when it is
  the current task (bold markers: 88.0 / 93.0 / 80.5) and then collapses to $\sim$2--4 F1
  once the next task is trained: the rows of Table~\ref{tab:forgetting-matrix} read left
  to right. Pilot result (seed 42).}
  \label{fig:forgetting-curves}
\end{figure}

\subsection{Is the Locus Causal? A Depth-Targeting Probe}
\label{sec:results-key-ablation}

Before evaluating LexSlot itself, we ask whether the diagnosed head/late locus is
\emph{causal}: does concentrating \emph{any} continual-learning mechanism there beat
spreading it uniformly across depth? We answer this with a clean depth-scaled consolidation
probe (a Fisher-weighted penalty whose per-bucket strength is either concentrated on the
head and late layers or spread equally across every depth) on DIL, three seeds each
(Table~\ref{tab:ablation}). The result is decisive. The \emph{uniform} variant, which applies
an equal penalty to every depth bucket (including the stable input and early encoders the
targeted schedule leaves free, and hence if anything a \emph{larger} total budget)
\emph{collapses in accuracy} to $42.6$ AA, roughly half the targeted variant and barely above the
naive lower bound, whereas concentrating the same budget on the head/late locus recovers
$84.7$. We are precise about the mechanism: the uniform variant does not forget \emph{more}; its
backward transfer ($-1.8$) is in fact the mildest in the table; it simply cannot \emph{fit} the
tasks when an equal penalty stiffens every depth, including the input encoders that should stay
plastic. So what the probe establishes is a budget-placement claim: a fixed consolidation budget
is only \emph{usable} when concentrated on the head/late locus, and is wasted (to the point of
under-fitting) when spread evenly. The \emph{head-only} ($86.1$) and \emph{late-only} ($86.2$)
restrictions each recover essentially the full accuracy, indicating the head term carries most of
the effect and the late-layer term is largely redundant with it. The probe is therefore
consistent with the diagnosis and is the strongest available causal evidence short of a direct
freezing intervention, which we flag as the cleaner control (a freeze of the high-displacement
bucket) for future work; it does not, on its own, isolate forgetting from capacity. It motivates LexSlot's design directly: place capacity at the head/late
locus, leave the rest frozen; and LexSlot's own placement ablation (\S\ref{sec:results-lexslot})
re-confirms the same targeting effect with the slot mechanism (hypotheses
H\textsubscript{target}/H\textsubscript{head} of \S\ref{sec:method-component-hypotheses}).

\subsection{The Proposed Remedy: LexSlot}
\label{sec:results-lexslot}

The proposed remedy, \textbf{LexSlot} (\S\ref{sec:method-lexslot}), protects the
diagnosed head/late locus with an \emph{isolated slot memory}, and its headline configuration
pairs that isolation with a compact 200-exemplar replay buffer (the DocCL-hybrid setting). On the domain-incremental scenario (LayoutLMv3, three seeds) the
isolated configuration reaches \textbf{AA $87.3 \pm 1.0$ with backward transfer $-2.3 \pm 1.2$}
(seeds 42/7/123: $88.4$/$87.2$/$86.3$).
Two readings stand out. First, its accuracy \emph{approaches the replay methods} (ER $87.9$, DER++
$88.0$, C-Flat++ $88.4$) and the joint oracle ($88.7$) on DIL, within about a point, the seeds
overlapping, and clearly exceeds the head/late consolidation probe ($84.7$,
\S\ref{sec:results-key-ablation}), with an order-of-magnitude smaller replay buffer than the
plain replay methods: most of the protection
comes structurally from freezing each task's slots, with the small buffer closing the remaining gap.
Second, its
forgetting ($-2.3$) is \emph{lower than the plain replay methods} (ER $-2.8$, DER++ $-3.4$),
with only the sharpness-aware C-Flat++ ($-1.8$) below it. So LexSlot is competitive
with replay on accuracy and better than plain replay on forgetting, at a qualitatively lower memory
cost; it does not, on the present three seeds, \emph{beat} the strongest replay variant on
accuracy. Table~\ref{tab:lexslot-ablation} reports the two pre-registered LexSlot ablations.

\emph{Sharing (isolation vs.\ graded sharing).} Letting lexically similar tasks co-train shared
slots (\emph{soft}) reaches $86.3 \pm 0.6$ / $-3.8 \pm 2.0$, \emph{below} the isolated \emph{off}
configuration ($87.3 \pm 1.0$ / $-2.3 \pm 1.2$) on both axes over three seeds. The putative
vocabulary-overlap transfer does not materialise: on this benchmark, allowing two tasks to write the
same slots costs more in interference than it recovers in shared-rule refinement. This is why
\emph{off} (isolation) is presented as the method and \emph{soft} as the control: the measurement
selects isolation.

\emph{Depth (targeting vs.\ uniform).} Placing slots only at the diagnosed head/late locus
($86.9$ / $-1.5$) beats spreading them across every layer (\emph{uniform}, $84.8$ / $-2.1$) by
$2.1$ AA with less forgetting, reproducing, now with the slot mechanism, the same targeting
effect the depth-scaled consolidation probe shows (\S\ref{sec:results-key-ablation}). LexSlot's
gain comes from \emph{where} the mechanism attaches: capacity placed where forgetting lives
outperforms the same capacity spread uniformly. The effect is smaller than for the consolidation
probe (which collapses to $42.6$ under uniform treatment), as expected: additive zero-initialised
slots away from the locus are near-inert rather than actively harmful, but its direction confirms
the diagnosis-driven placement.

\begin{table}[!htbp]
  \centering
  \caption[LexSlot ablation: sharing and depth]{LexSlot ablations on the domain-incremental
  scenario (LayoutLMv3). \emph{Sharing}: isolating each task's slots (\emph{off}) beats graded
  lexical sharing (\emph{soft}) on both AA and backward transfer over three seeds, so the
  putative vocabulary-overlap transfer does not outweigh slot interference: isolation is the
  selected method. \emph{Depth}: slots concentrated on the diagnosed head/late locus versus
  spread uniformly across depth (targeting the locus beats spreading by $2.1$ AA), reproducing the
  depth-targeting probe's effect with the slot mechanism. The sharing rows are mean\,$\pm$\,std over three seeds (42/123/7);
  the depth-ablation rows are a single seed (seed $42$) pending the grid, marked ``(1\,seed)''.
  Multi-backbone values await the pending grid (Chapter~\ref{ch:conclusions}).}
  \label{tab:lexslot-ablation}
  \IfFileExists{generated/table_lexslot_ablation.tex}{\input{generated/table_lexslot_ablation}}{%
  \begin{tabular}{llcc}
    \toprule
    \textbf{Axis} & \textbf{Setting} & \textbf{AA} & \textbf{BWT} \\
    \midrule
    \multirow{2}{*}{Sharing (depth = head/late)}
      & \textbf{off (isolated, selected)} & $\mathbf{87.3 \pm 1.0}$ & $\mathbf{-2.3 \pm 1.2}$ \\
      & soft (graded by $S$)              & $86.3 \pm 0.6$          & $-3.8 \pm 2.0$ \\
    \midrule
    \multirow{3}{*}{Depth (seed 42)}
      & head/late (targeted)              & $86.9$~{\scriptsize (1\,seed)} & $-1.5$~{\scriptsize (1\,seed)} \\
      & head-only                         & $87.1$~{\scriptsize (1\,seed)} & $-3.2$~{\scriptsize (1\,seed)} \\
      & uniform (all depths)              & $84.8$~{\scriptsize (1\,seed)} & $-2.1$~{\scriptsize (1\,seed)} \\
    \bottomrule
  \end{tabular}}
\end{table}

The headline isolated and shared configurations are now measured over three seeds; the
depth-ablation rows (head-only, uniform) are a single seed pending the grid. As with the
depth-targeting probe, a
head-only restriction ($87.1$, single seed) already recovers essentially the full head/late
result, suggesting the late-layer slots are largely redundant with the head slots; a simpler
head-only variant is a natural future simplification. The remaining scope is the multi-backbone
(LiLT/BROS/BERT) confirmation, still the pending grid, and class-incremental learning is a known
hard case for the lexical gate (same-dataset sessions collapse the signature contrast). What the
present evidence establishes is that the head/late diagnosis admits a \emph{second},
mechanistically distinct remedy that on a domain shift is competitive with replay at a small
fraction of its exemplar budget,
with lower forgetting than the plain replay methods, supporting the thesis's central claim that
localising forgetting is a practical instrument for \emph{designing} continual methods, not just
one method.

\subsection{Generalisation Across Backbones}
\label{sec:results-backbone}

\S\ref{sec:results-diagnosis} established that the \emph{forgetting locus} is
architecture-general (the head-dominant displacement and the monotonic CKA depth gradient
reproduce on BERT, LiLT, and BROS). A complementary question is whether the \emph{method
ordering} (replay $\gg$ regularisation $\gg$ naive) also transfers off LayoutLMv3.
Table~\ref{tab:backbone-byscenario} reports the secondary-backbone runs available:
the \emph{complete} text-only BERT slice (the full method grid at three seeds on CIL-CORD, DIL,
and Mixed, plus the naive lower bound on all five scenarios), and the four classical methods on
the decoupled-stream LiLT backbone (DIL and Mixed, three seeds).
The ordering is preserved on both secondaries. On LiLT, replay (ER $85.7$, DER++ $84.4$ on DIL)
dominates by a wide margin, regularisation (EWC $48.4$) sits in between, and naive ($40.1$) is
the floor, the same pattern measured on LayoutLMv3 (Table~\ref{tab:main-comparison}), with the
absolute replay level a few points lower on the smaller-capacity backbone. On text-only BERT the
full grid reproduces the same three-tier structure at a uniformly lower level: replay
(ER + C-Flat++ $78.3$, DER++ $77.4$, ER $77.1$ on DIL) reaches the joint upper bound measured on
the same backbone ($77.2$), regularisation barely moves the naive floor (EWC $46.6$ vs.\ naive
$39.4$), and the prompt/LoRA families remain at or below naive ($34$--$39$), so the
replay-dominant ordering, the regularisation gap, \emph{and} the prompt-family failure all
survive the removal of the layout and vision modalities entirely. Remaining coverage is
explicit: LiLT is run on the two scenarios the secondary grid has reached, and the BROS backbone
has no runs yet; completing that matrix is the pending rented-GPU grid
(Chapter~\ref{ch:conclusions}). What the present rows establish is that the benchmark's central
empirical ordering is not an artefact of one backbone, or of multimodality itself.

\begin{figure}[!htbp]
  \centering
  \IfFileExists{figures/backbone_ordering.pdf}{%
    \includegraphics[width=0.92\linewidth]{backbone_ordering.pdf}}{}
  \caption[Replay-dominant ordering across backbones]{The replay-dominant ordering on
  DIL across backbones (mean over three seeds, from the aggregated run table). The
  three-tier structure (replay near the joint oracle, regularisation barely above the
  naive floor) reproduces on the text-only BERT backbone at a uniformly lower level and
  on LiLT where cells exist; C-Flat++ and Joint are not yet run on LiLT.}
  \label{fig:backbone-ordering}
\end{figure}

\begin{table}[!htbp]
  \centering
  \caption[Method comparison across backbones]{Average accuracy (AA) across backbones, by
  scenario (mean\,$\pm$\,std over three seeds). LiLT (decoupled text\,$+$\,layout) is run on DIL
  and Mixed; text-only BERT carries the full method grid on CIL-CORD, DIL, and Mixed plus naive
  on all five scenarios. The replay-dominant ordering of the LayoutLMv3 grid
  (Table~\ref{tab:main-comparison}) is preserved on both secondaries. Cells marked ``--'' are not
  yet run: the BROS backbone and LiLT on the remaining scenarios are the pending grid
  (Chapter~\ref{ch:conclusions}).}
  \label{tab:backbone-byscenario}
  % 5-scenario table is wider than \textwidth; wrap in \resizebox to fit and stay centred.
  \IfFileExists{generated/table_backbone_byscenario.tex}{\resizebox{\textwidth}{!}{\input{generated/table_backbone_byscenario}}}{%
  \begin{tabular}{llc}
    \toprule
    \textbf{Backbone} & \textbf{Method} & \textbf{DIL / Mixed} \\
    \midrule
    LiLT & ER & $85.7$ / $57.0$ \\
    LiLT & DER++ & $84.4$ / $54.2$ \\
    \bottomrule
  \end{tabular}}
\end{table}

\section{Ablation Studies}
\label{sec:results-ablation}

The depth-targeting probe restricts a depth-scaled consolidation penalty to a subset of
depths to test whether the diagnosed head/late target is what matters, a causal check on the
diagnosis, independent of the proposed remedy. The variants (head-only / late-only / uniform /
full) were run on DIL over three seeds
(Table~\ref{tab:ablation}). The result is decisive: the \emph{uniform} variant collapses
to $42.6$ AA, less than half the targeted variants and barely above naive, whereas
\emph{head-only} ($86.1$) and \emph{late-only} ($86.2$) each recover essentially the full
result and slightly edge the full schedule ($84.7$). This confirms both
H\textsubscript{target} (\emph{where} the capacity is spent dominates) and
H\textsubscript{head} (the head/late locus is the effective target); the interpretation is
developed in \S\ref{sec:results-key-ablation}, and LexSlot's own placement ablation
(\S\ref{sec:results-lexslot}) reproduces the same effect with the slot mechanism. Two honest
limits bound the claim. First,
that head-only and late-only each \emph{match} the full schedule means the extra late-layer
term is largely \emph{redundant} with the head term on this
scenario: a simpler head-only target would be near-equivalent here, which is exactly the
configuration LexSlot adopts. Second, this probe is on DIL, the scenario where head/late
targeting is effective; the same contrast on the CIL-CORD growing-head scenario is
uninformative (no variant clears $\sim$18 AA, since no method works there), so the targeting
claim is established for the domain shift and left open for the harder scenarios.

\begin{table}[!htbp]
  \centering
  \caption[Depth-targeting probe]{Depth-targeting probe on the DIL scenario
  (three seeds): a depth-scaled consolidation penalty restricted to different depths, used to
  test whether the diagnosed locus is causal. Concentrating on the locus (\emph{head
  only} ($86.1$) or \emph{late only} ($86.2$)) recovers essentially the full result and
  slightly edges the full head$+$late schedule ($84.7$), while the \emph{uniform}
  across-depth variant collapses to $42.6$. This confirms the targeting hypotheses
  H\textsubscript{target} and H\textsubscript{head} (\S\ref{sec:results-key-ablation}): the
  gain comes from \emph{where} capacity is spent, not merely how much: the principle LexSlot
  is built on.}
  \label{tab:ablation}
  % Auto-loaded from generated/table_ablation.tex (scenario: dil); the inline table below
  % is the fallback mirror.
  \IfFileExists{generated/table_ablation.tex}{\input{generated/table_ablation}}{%
  \begin{tabular}{lcc}
    \toprule
    \textbf{Targeted depth} & \textbf{AA} & \textbf{BWT} \\
    \midrule
    Head only                   & $86.1 \pm 1.7$ & $-5.4 \pm 2.5$ \\
    Late layers only            & $86.2 \pm 1.5$ & $-5.2 \pm 2.0$ \\
    Uniform (all depths)        & $42.6 \pm 1.4$ & $-1.8 \pm 0.3$ \\
    Head $+$ late (full)        & $84.7 \pm 1.7$ & $-4.6 \pm 1.9$ \\
    \bottomrule
  \end{tabular}}
\end{table}

\section{Computational Efficiency}
\label{sec:results-compute}

Table~\ref{tab:compute} reports the wall-clock and memory cost of the baseline methods on the
CIL-CORD scenario. The only strictly hardware- and batch-independent quantity is the
number of \emph{trainable parameters}, essentially identical across these methods
($\sim$125\,M). The wall-clock and peak-memory figures, by contrast, were
collected across heterogeneous hardware and batch sizes during the grid and so are
\emph{indicative rather than strictly comparable}: replay's buffers and second
forward/backward pass add genuine memory and time (ER and DER++ carry a replay buffer,
and DER++ additionally caches output logits). The proposed \textbf{LexSlot} is
parameter-efficient in the opposite direction: it \emph{freezes} the backbone and
trains only a small slot memory (head logit-slots $+$ low-rank late-layer adapters, a $\sim$1--2\%
parameter overhead over the frozen backbone; hyperparameters in
Appendix~\ref{app:hyperparameters}), and its headline configuration pairs that frozen-backbone
slot memory with a compact 200-exemplar replay buffer, an order of magnitude smaller than the
plain replay methods' buffers, so its standing
advantage is qualitative: replay-level retention on the domain shift (\S\ref{sec:results-lexslot})
with a frozen backbone and a small fraction of the rehearsal data. Its CIL-CORD wall-clock/memory numbers are not in
this table (LexSlot is measured on DIL so far). ``Run'' denotes one full continual scenario.

\begin{table}[!htbp]
  \centering
  \caption[Computational overhead]{Wall-clock, peak memory, and trainable-parameter count
  of the baseline continual-learning methods on CIL-CORD. ``Run'' $=$ one full continual
  scenario. Trainable parameters ($\sim$125.4\,M) are essentially identical across these
  full-fine-tuning methods and are the only strictly comparable column. Wall-clock and
  peak-memory were collected across heterogeneous hardware and batch sizes during the grid, so
  they are indicative, not strictly comparable: replay's buffers add real memory. The proposed
  LexSlot (\S\ref{sec:results-lexslot}) instead freezes the backbone and trains only a small slot
  memory, paired with a compact 200-exemplar replay buffer (an order of magnitude smaller than
  the plain replay methods'); its CIL-CORD cost is not listed (measured on DIL so far).}
  \label{tab:compute}
  % Auto-loaded from generated/table_compute.tex; the inline table below mirrors the numbers.
  \IfFileExists{generated/table_compute.tex}{\input{generated/table_compute}}{%
  \begin{tabular}{lrrr}
    \toprule
    \textbf{Method} & \textbf{Trainable params} & \textbf{Time / run (s, indic.)} & \textbf{Peak mem (MB, indic.)} \\
    \midrule
    Naive          & 125.4 M & 614.5  & 10533 \\
    EWC            & 125.4 M & 254.6  & 20386 \\
    LwF            & 125.4 M & 392.2  & 20894 \\
    ER             & 125.4 M & 174.5  & 27144 \\
    DER++          & 125.4 M & 298.5  & 35540 \\
    \bottomrule
  \end{tabular}}
\end{table}

\section{Extended Scenarios: Scale and Language Shift}
\label{sec:results-extended}

Two further scenarios stress the benchmark along axes the main grid does not isolate:
\emph{scale} (a large, growing label space) and \emph{pure language shift} (a fixed label
space with only the input language changing). The cross-lingual scenario is run with the
classical method suite plus O-LoRA and CODA-Prompt, whereas on the large-scale
WildReceipt scenario, for compute-budget reasons, only the classical continual-learning
methods and the joint oracle are reported. The results (Figure~\ref{fig:newscenarios-aa})
are decisive, and the cross-lingual scenario in particular provides the strongest single
corroboration of the output-side forgetting thesis.

\paragraph{Scale: WildReceipt class-incremental learning.} The \texttt{cil\_wildreceipt}
scenario grows a single head over four sessions to cover 24 entity classes ($49$ BIO
tags), the largest growing output space in the benchmark. Forgetting here is severe and,
unlike DIL, \emph{replay does not rescue it}: naive reaches only $20.8$ AA with BWT
$-84.5$, and the replay methods barely move the needle (ER $19.9$ / $-79.4$, DER++
$20.2$ / $-84.0$), all far below the joint oracle ($33.9$). The best non-oracle accuracy
is just $\sim$$20.8$ (LwF and naive). The lesson is that class-incremental learning
\emph{at scale} is the hardest regime: when the head must keep growing to admit dozens of
new classes, the head-growth bottleneck dominates, and a replay buffer over past inputs
cannot compensate for an output space that keeps expanding. This mirrors, at larger
scale, the CIL-CORD difficulty in the main grid.

\paragraph{Pure language shift: XFUND cross-lingual DIL.} The \texttt{dil\_xlingual}
scenario presents five languages in sequence (de\,$\rightarrow$\,es\,$\rightarrow$\,fr\,$\rightarrow$\,it\,$\rightarrow$\,zh)
under a \emph{fixed} unified label space: only the input language changes, never the set
of output classes. The headline finding is that forgetting is \emph{near zero for every
method}, including naive (BWT $-6.6$); regularisation and replay show forgetting at or
below zero (EWC $-9.6$, ER $+1.6$, DER++ $+0.5$, i.e.\ slightly \emph{positive} backward
transfer). On accuracy, naive already reaches $72.9$ AA, DER++ $80.3$, and the replay
methods even edge \emph{past} the joint oracle ($78.5$), exactly as the head-locus account
predicts: with the output space fixed there is almost no head-forgetting for any head/late
remedy to repair, so head-protection neither helps nor hurts here, which is itself corroborating
evidence. This is direct support for the
output-side / head-locus thesis. When the label space is held fixed and only the input
language shifts, the classifier head never has to relearn its output distribution, so the
head-concentrated forgetting mechanism is barely triggered and forgetting is minimal.
Contrast this with format/schema shift (regular DIL: naive BWT $-73.1$) and with
class-growth (CIL-CORD and WildReceipt: BWT $\approx-85$ to $-94$), where the head
\emph{must} change and forgetting is catastrophic. Read together, the three regimes form
a clean gradient: near-zero forgetting under pure input-language shift, severe forgetting
under output-space change, which is strong corroboration that forgetting in continual
document IE is driven by change at the \emph{output side} (the classifier head), not by
input-representation drift \emph{per se}.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth,height=0.5\textheight,keepaspectratio]{bench_newscenarios_aa.pdf}
  \caption[Extended scenarios: scale and language shift]{Average accuracy on the two
  extended scenarios. \emph{Left:} WildReceipt class-incremental learning (scale axis;
  classical methods and joint oracle only, the heavier methods not run here for
  compute-budget reasons), where forgetting is severe and even replay stays near the naive
  lower bound, far below the oracle. \emph{Right:} XFUND cross-lingual DIL (pure language
  shift, fixed label space), where forgetting is near
  zero for every method: naive already reaches $72.9$ AA and replay edges past the
  oracle, because the fixed output space never forces
  the head to relearn its distribution. The contrast corroborates the output-side /
  head-locus forgetting thesis.}
  \label{fig:newscenarios-aa}
\end{figure}

\section{Extended Falsification Results and the Replay Control}
\label{sec:results-falsification-ext}

The experiments in this section post-date the main grid and sharpen its central negative
result. They ask, in sequence: \emph{what exactly} is the mechanism of forgetting
(a root-cause analysis of the baselines), whether any \emph{cheap} correction of that
mechanism works (a pre-registered kill-test programme), whether \emph{compressed} replay can
retain the one intervention that does work (the CoLaR control), and whether task-specific
capacity can be added on top of it (the CoLaSlot gates). All runs use the DIL stream on
LayoutLMv3 unless noted; single-seed results are flagged as such.

\paragraph{Root cause: the readout-marginal snap.}
A per-boundary root-cause analysis of the core baselines (modality-ablated evaluation,
per-class F1, token confusion, and Fisher displacement at every task boundary) localises the
failure to the readout. For the failing methods, the head's \emph{output marginal} on old
tasks snaps to the just-trained task's gold label marginal (cosine $0.997$--$0.998$ for
naive and LwF at the first boundary, versus $0.60$ to the old task's own marginal); replay
methods stay at $0.99$--$1.00$ to their own gold marginal. Classes that are never
re-exercised (\textsc{key}, \textsc{header}) are extinguished to exactly $0.0$ F1 under
\emph{every} modality mask, and the background \textsc{o} class collapses too (row accuracy
$0.00\%$, invisible to entity-level F1). Trunk displacement is front-loaded and functionally
minor, and no modality pathway is the culprit: forgetting here is a \emph{readout-marginal
snap on a class-asymmetric substrate}, not representation erasure.

\paragraph{Pre-registered kill-tests: every cheap correction is a null.}
If the snap were merely a mis-calibrated prior, eval-time or marginal-level corrections
should reverse it. Five kill-tests, registered with explicit support/kill bars before any
run executed, though we note the registration and adjudication were self-administered rather
than third-party, close this hole. Eval-time readout repair (marginal matching, prior-ratio
correction, and per-document EM re-estimation) recovers at best $-0.1$ AA: the saturation
probe shows the head has already annihilated the recoverable mass (old-class probability
mass ${\sim}10^{-6}$, two to three orders below the $10^{-3}$ branch threshold), so the
information is destroyed before any correction can act. Marginal-level \emph{training}
objectives fare no better: a KL anchor to the prior-task label mixture and a
balanced-softmax logit adjustment both leave the extinguished classes at $0.0$ F1. A
frozen-trunk variant reproduces old-class extinction with the trunk fixed, though its poor
acquisition makes it invalid as a clean effect-size confirmation. The consolidated verdict:
the snap is a \emph{symptom}, not an invertible transformation: only task-consistent
re-exercise (replay) prevents it. This closes the ``did you try cheap recalibration''
objection with registered nulls rather than absence of evidence.

\paragraph{Whole-document consistency is the necessary replay ingredient.}
Given that only replay works, \emph{what about replay} is load-bearing? A falsification
ladder over latent replay at a fixed frozen boundary (layer $k=8$) isolates it. Replaying
\emph{real} banked activations of whole documents retains AA $87.3$ (converged). Every
summarised variant collapses to naive level: a rank-16 spectral summary of the same store
reaches $41.9$, per-class Gaussians (full dimension, no truncation) $39.4$, and even
\emph{real} k-means centroids of the same activations $36.7$, so the failure is not
synthesis fidelity. The single-variable control makes the mechanism explicit: four whole
replayed documents retain AA $63.8$ (at a matched five-epoch budget), while the same
features pasted onto decoupled layout carriers retain $36.7$. The
(feature, position, label) binding of an intact document is the necessary ingredient;
summaries that break it fail regardless of how faithful they are pointwise. A bounded
positive probes the storage side of the same law: replaying pseudo-labelled \emph{public}
documents (zero private bytes) recovers dense-label tasks (FUNSD $76.1$) but not tasks
whose feature region the proxy pool does not cover (SROIE ${\sim}7$), topping out at AA
$58.9$, a coverage-limited partial transfer, not a method.

\begin{figure}[!htbp]
  \centering
  \IfFileExists{figures/falsification_ladder.pdf}{%
    \includegraphics[width=0.9\linewidth]{falsification_ladder.pdf}}{}
  \caption[The latent-replay falsification ladder]{The latent-replay falsification
  ladder at a fixed frozen boundary (layer $k{=}8$, DIL). Only whole-document replay
  (blue) retains accuracy; every summarised representation, including \emph{real}
  $k$-means centroids, collapses to the naive level, and the matched-budget
  single-variable control (whole documents $63.8$ vs.\ the same features on decoupled
  carriers $36.7$) isolates whole-document (feature, position, label) binding as the
  necessary ingredient. Single-seed diagnostic runs (seed 42).}
  \label{fig:falsification-ladder}
\end{figure}

\paragraph{CoLaR: the constructive control.}
The negatives above could be effort-limited. CoLaR (compressed latent replay) shows they are
not: per-\emph{document} rank-$r$ SVD factors respect whole-document binding (unlike the
pooled feature space, individual documents \emph{are} low-rank), giving AA $87.6$ at
$60$\,MB ($2.7\times$ smaller than raw replay at equal document count; final-task row
$[89.2, 76.3, 97.2]$ on FUNSD/SROIE/CORD), within $1.4$ points of the joint oracle.
Compression that respects the consistency law works; every marginal summary collapses.
CoLaR is a \emph{control}, not a claimed contribution. Attempts to improve it obey a
conservation law: class-balanced replay losses ($87.1$), coverage-based exemplar selection
($86.8$), their combination ($87.3$), and entity-weighted compression ($87.1$) all trade
FUNSD surplus for SROIE roughly $1{:}1$ without moving AA; a three-seed check confirms no
lever beats the base ($87.8$ base $=$ balanced $87.8$ $>$ $k$-center $87.0$). Read-side
extensions are clean negatives: blending a $k$NN readout over the stored latents is inert at
$\lambda=0.3$ and destructive at $\lambda=1.0$ (AA $46.2$), and Benna--Fusi metaplastic
consolidation is dose-dependently harmful ($85.9$ at $m=1$, $79.5$ at $m=3$), consistent
with the root cause: the readout must be \emph{repaired by re-exercise}, not averaged with a
weaker reader or stiffened.

\begin{figure}[!htbp]
  \centering
  \IfFileExists{figures/colar_pareto.pdf}{%
    \includegraphics[width=0.78\linewidth]{colar_pareto.pdf}}{}
  \caption[Replay memory versus retention]{Replay memory versus retention (DIL,
  log-scale memory axis). CoLaR (blue) matches raw latent replay's accuracy at
  $2.7\times$ less storage because its per-document SVD factors preserve whole-document
  binding; the aggressive compressions at ${\sim}0.4$\,MB all break that binding and
  collapse. Memory figures are serialized replay-store bytes as instrumented per run.}
  \label{fig:colar-pareto}
\end{figure}

\paragraph{Task-capacity gates on CoLaR (single-seed, provisional).}
Two pre-registered gates ask whether task-specific head capacity can break the conservation
law. CoLaSlot-R (owner-routed additive head slots) raises the matched five-epoch, five-document
baseline by $+14.0$ AA but regresses the final domain (CORD $-0.84$), failing its
pre-registered no-domain-below$-0.5$ guardrail; an exhaustive test-blind calibration of its
scalar abstention margin found no threshold giving every domain both ${\geq}50\%$ coverage
and ${\geq}99\%$ accepted accuracy, closing that design. Its successor CoLaSlot-RF keeps
training exactly CoLaR-identical and refits frozen owner-specific head residuals only
\emph{after} each task, reading them only for prior-task routes. The saved dual evaluation
shows that its AA $72.4$ and final row $[67.6,52.7,96.9]$ are exactly equal to its own
same-state slots-off control; the previously cited $60.8$ control came from a different run.
Hard-label refit losses near $10^{-5}$ explain the null effect. Replacing labels with
acquisition-logit anchors restores a training signal, but reduces AA by $0.43$ and SROIE by
$0.99$ through lower precision. Both variants therefore fail the pre-registered gate; no
full-budget or additional-seed run is warranted.

\begin{table}[!htbp]
  \centering
  \caption[CoLaSlot capacity-gate verdicts]{Pre-registered CoLaSlot capacity gates on
  CoLaR (matched DIL $d{=}5$/$r{=}64$ seed-42 cells; single-seed diagnostic budget).
  Wrong routes are destructive, correct routes are inert; head residuals on compressed
  replay are closed in their current form.}
  \label{tab:capacity-gates}
  \IfFileExists{generated/table_capacity_gates.tex}{%
    \input{generated/table_capacity_gates}}{}
\end{table}

\section{Discussion}
\label{sec:results-discussion}

\paragraph{What the diagnosis means.}
The diagnosis (\S\ref{sec:results-diagnosis}) gives a clear, if sobering, picture:
under naive sequential fine-tuning LayoutLMv3 forgets catastrophically, and the
forgetting concentrates, by both per-token CKA drift and old-task-Fisher-weighted
displacement, in the \emph{classifier head and the late encoder layers}, not in the
input encoders. We are careful not to over-claim: because LayoutLMv3 is single-stream
we do \emph{not} assert a ``fusion-specific'' (or its absence as a) finding, since
fusion is not a measurable parameter component here; the cross-condition magnitude
test, moreover, was underpowered by construction, so the head/late locus rests instead
on the per-component Fisher displacement test (classifier head dominant, $p<0.01$), the
CKA depth gradient, and the displacement bars across all four backbone conditions
(LayoutLMv3, BERT, LiLT and BROS), whose displacement-by-depth profiles are
statistically indistinguishable, establishing that the pattern is neither
multimodal-specific nor an artefact of a single architecture. The actionable
consequence is robust to all of this: the
forgetting locus is output-facing, so remedies that protect or re-expose the upper
layers and the classifier, of which experience replay is the simplest instance, and
the head/late-targeted LexSlot the diagnosis-grounded one, are the natural
intervention, which also explains \emph{why} replay is so effective below
(Figure~\ref{fig:replay-mechanism}).

\begin{figure}[!htbp]
  \centering
  \begin{tikzpicture}[
      font=\small, node distance=4mm,
      stage/.style={draw, rounded corners, align=left, inner sep=4pt, text width=38mm, font=\scriptsize, minimum height=22mm},
      flow/.style={-{Latex[length=2.5mm]}, thick},
      note/.style={draw, rounded corners, align=left, inner sep=4pt, text width=\linewidth, font=\scriptsize}]
    \node[stage, fill=blue!6] (diag)
      {\textbf{Diagnosis (\S\ref{sec:results-diagnosis}).} Forgetting concentrates at the
       classifier head and late layers; the head dominates Fisher importance
       ($2.2\!\times\!10^{-1}$) and drifts most (CKA $0.16$). Inputs stay stable.};
    \node[stage, fill=blue!12, right=8mm of diag] (mech)
      {\textbf{Mechanism: replay.} A small buffer of past examples is replayed each step.
       Old-class targets keep producing gradient at the output head, so the head is not
       overwritten as new tasks arrive.};
    \node[stage, fill=green!10, right=8mm of mech] (out)
      {\textbf{Outcome (\S\ref{sec:results-remedy}).} The head stays aligned. On DIL: ER/DER++
       AA $\approx 88$, BWT $\approx 0$, matching the joint oracle ($88.7$); forgetting
       essentially eliminated.};
    \draw[flow] (diag) -- (mech);
    \draw[flow] (mech) -- (out);
    \node[note, fill=gray!8, below=4mm of mech.south, anchor=north]
      {\textbf{The diagnosis-grounded alternative.} Because the locus is output-facing, a
       mechanism that targets the head/late layers \emph{directly}: the proposed
       \textbf{LexSlot}, an isolated slot memory paired with a compact 200-exemplar replay
       buffer, matches replay-level accuracy on DIL
       ($87.3$ vs the oracle's $88.7$) and forgets \emph{less} than the plain replay methods
       ($-2.3$ vs $-2.8$/$-3.4$), \emph{with an order-of-magnitude smaller exemplar budget}. By contrast EWC
       constrains all weights broadly (partial help, mainly under domain shift) and LwF
       distils old-class logits that decay as the head grows (no measurable benefit here).};
  \end{tikzpicture}
  \caption[Why replay works]{From diagnosis to result: because forgetting is output-facing,
  replay's re-exposure of old-class targets keeps the classifier head aligned, which
  explains its empirical win (both ER and DER++ reach DIL BWT $\approx 0$). The
  diagnosis-grounded method, LexSlot, an isolated slot memory at the head/late layers,
  approaches the oracle and replay on the DIL forgetting gap ($87.3$ vs the oracle's $88.7$),
  forgets less than the plain replay methods, and does so \emph{with a compact 200-exemplar buffer}
  (frozen backbone, an order-of-magnitude smaller exemplar budget than plain replay).}
  \label{fig:replay-mechanism}
\end{figure}

\paragraph{What the benchmark shows.}
Across the three scenarios the accuracy ordering is unambiguous: replay $\gg$
regularisation $\gg$ distillation. Both replay methods essentially solve the
domain-incremental scenario (ER AA $87.9$/BWT $-2.8$; DER++ $88.0$/$-3.4$, both matching
the joint oracle of $88.7$) and lead on Mixed. We interpret this DIL near-oracle result
\emph{cautiously and with per-class evidence}, because the unified DIL schema is
VALUE-dominant (KEY originates only from FUNSD/SROIE; \S\ref{sec:eval-seeds}), so a
high aggregate accuracy could in principle reflect retention of the dominant VALUE
class rather than genuine cross-domain retention; we report per-class F1 and per-task
label frequencies and read the replay result as real cross-domain retention only if the
sparse KEY class is itself retained. The convergence training surfaces a second, sharper
lesson: \emph{backward transfer and accuracy must be read together}. EWC trained to
convergence achieves the \emph{lowest forgetting} on CIL-CORD and Mixed, yet its accuracy
\emph{collapses} (CIL-CORD $0.5$, Mixed $22.0\pm7.6$): the Fisher penalty over-protects
the old classes and starves the new ones, so a low BWT here signals failure-to-learn, not
success. LwF tracks the naive lower bound throughout. That the two replay variants land so
close together suggests the benefit comes from \emph{rehearsing past examples} per se
rather than from the logit-distillation term DER++ adds on top. The dense
token-classification setting with a growing head is the likely reason distillation
(LwF) fails: the old-class logits it distils decay regardless once their classes stop
being reinforced. The scenario lens matters too: domain-incremental learning, with
distinct documents under a fixed label space, is the most tractable, whereas
fine-grained class-incremental learning over a single dataset (60 classes in one
growing head) is the hardest for every method, oracle included.

\paragraph{Threats to validity.}
Several caveats bound these conclusions. (i) All reported methods are measured and were
held \emph{fixed across all methods} under one stopping rule, so the method comparison is
internally valid (the small per-seed standard deviations indicate stable rankings). The
single bounded coverage gap is that the 2025 baselines and the prompt/LoRA methods
were not run on the large-scale WildReceipt scenario
(\S\ref{sec:results-extended}), where only the classical methods and the oracle are
reported; everything else, including C-Flat++ (\texttt{er\_cflat}), the depth-targeting
probe, and the cross-lingual scenario, is measured over three
seeds. The proposed LexSlot is measured on DIL/LayoutLMv3 (3 seeds); its other-scenario and
multi-backbone confirmation is the pending grid. The qualitative conclusions: replay strongest,
LexSlot approaching replay on the DIL gap with a small fraction of the exemplar budget, and the depth-targeting probe
confirming the head/late locus, do not depend on the unrun cells.
(ii) Each scenario uses
a single fixed task order in the main grid; the pilot showed order materially affects
forgetting ($\sim$6 BWT points), so the full run repeats the comparison under alternate
orders. (iii) Forward transfer is measured from the grid's zero-shot entries against the
single-task baselines (\S\ref{sec:results-bwt-fwt}) and is strongly negative by the
structure of the scenarios. (iv) The exploratory cross-condition test was underpowered by
construction; the head/late locus is instead established by the per-component Fisher test
($p<0.01$), the CKA depth gradient, and the displacement contrast across backbones. The
per-depth location permutation test is now available on the secondary backbones and
\emph{fails to distinguish} their forgetting profile from LayoutLMv3's ($p=0.38$ for
LiLT, $p=0.51$ for BROS), corroborating a shared locus. (v) The
DIL unified schema is VALUE-dominant with KEY only in FUNSD/SROIE, so every DIL result
is interpreted alongside per-class F1 and per-task label frequencies
(\S\ref{sec:eval-seeds}). (vi) The forgetting \emph{diagnosis} now spans four backbones:
the per-component localiser, CKA depth gradient and displacement profile were run on the
primary LayoutLMv3, the unimodal BERT baseline, and the LiLT and BROS secondary
backbones, and all four place forgetting at the head/late layers (the profile test cannot
separate them). What remains scoped to LayoutLMv3 is the \emph{full CL benchmark}
(AA/BWT/AF across the ten baselines and all scenarios): the LiLT/BROS retention-metric
sweep is a compute-bound follow-up on larger hardware, and the magnitude comparison of
$|\mathrm{BWT}|$ across backbones is underpowered at three seeds, so we claim a shared
\emph{location} of forgetting, not a shared \emph{amount}. None of these undercut the
robust findings: forgetting is a depth/head phenomenon \emph{across architectures},
replay (ER/DER++) is the strongest remedy and matches the joint
oracle on domain-incremental learning while LexSlot approaches that gap with an order-of-magnitude
smaller replay buffer, and forgetting
is near zero under pure language shift but catastrophic under output-space growth. They
do, however, delimit how far the exact numbers should be pushed.
