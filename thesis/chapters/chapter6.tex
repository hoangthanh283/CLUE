% =====================================================================
\chapter{Results and Discussion}
\label{ch:results}
% =====================================================================
% POPULATED FROM THE COMPLETED GRID. Section 6.1 (per-component diagnosis) is
% filled from the pilot; sections 6.2/6.3/6.4 are filled from the COMPLETE
% main grid (54/54 runs: naive, joint, EWC, LwF, ER, DER++ x 3 scenarios x 3
% seeds) aggregated by scripts/analyze_results.py -> generated/table_*.tex.
% The prompt/LoRA baselines (L2P, DualPrompt, CODA-Prompt, O-LoRA), the proposed
% DocCL remedy, and the component-targeting ablation are deferred (GATE A was
% inconclusive -> characterisation-only fallback) and remain explicit em-dash
% placeholders. Do NOT insert fabricated numbers for the deferred cells.
% =====================================================================

This chapter reports the empirical findings. The per-component diagnosis is the
headline result (\S\ref{sec:results-diagnosis}); the mechanism-targeted remedy is
then compared against the baselines (\S\ref{sec:results-remedy}), followed by
ablations (\S\ref{sec:results-ablation}), a computational-cost analysis
(\S\ref{sec:results-compute}), and a discussion of implications and threats to
validity (\S\ref{sec:results-discussion}).

\paragraph{Status of these results.} The six core continual-learning methods are
\emph{complete}: the full main grid of $54$ runs (six methods $\times$ three scenarios
$\times$ three seeds) finished without a single failed run. They were obtained under a
configuration adapted to a single 6\,GB GPU---three epochs per task and batch size
two---which is lighter than the ten-epoch full protocol of
Chapter~\ref{ch:experimental-setup} but was held fixed across every method, so the
\emph{comparisons} are apples-to-apples; the conclusions are stable across the three
seeds (small standard deviations throughout). What remains deferred is the
\emph{additional} method family rather than the core comparison: the prompt-based
(L2P, DualPrompt, CODA-Prompt) and LoRA-based (O-LoRA) baselines, and the proposed
DocCL remedy with its component-targeting ablation, were not run because the Week-4
diagnosis (\S\ref{sec:results-stability}) was inconclusive and triggered the
characterisation-only fallback (\S\ref{sec:method-fallback}). Cells for these deferred
methods are marked with an em-dash (``--'') and explained in place, and are never
filled with estimated values.

\section{Where Forgetting Lives: The Per-Component Diagnosis}
\label{sec:results-diagnosis}

This section reports how forgetting distributes across the text, visual, layout,
and fusion components under naive sequential fine-tuning, for the four diagnostic
conditions C1--C4, and tests the hypotheses of
\S\ref{sec:method-diag-hypotheses}. The pilot trains LayoutLMv3-base naively and
sequentially on FUNSD\,$\rightarrow$\,CORD\,$\rightarrow$\,SROIE, recording the
performance matrix $R[i,j]$, per-layer \gls{cka} between consecutive checkpoints,
and per-group empirical Fisher information at every task boundary.

\subsection{Per-Component Forgetting Profile}
\label{sec:results-profile}

Under naive sequential fine-tuning the model learns each task well in isolation but
retains almost nothing of earlier tasks. Table~\ref{tab:forgetting-matrix} and
Figure~\ref{fig:forgetting-matrix-viz} show the
performance matrix for the full multimodal condition (C4): the diagonal is strong
(88.0 / 93.0 / 80.5 entity-F1), but the off-diagonal collapses to roughly $2$ F1.
After learning CORD, FUNSD performance falls from $88.0$ to $1.8$---an $86$-point
drop in a single task step. The text\,+\,layout condition (C3) shows the identical
pattern ($88.7\rightarrow0.3\rightarrow1.7$). Forgetting in this setting is therefore
not gradual but essentially total within one boundary, which is what motivates the
continual-learning treatment of the remaining sections.

Attributing this collapse to specific parameters, Table~\ref{tab:fisher-component}
reports the empirical Fisher information per parameter group, averaged over seeds and
task boundaries. The \emph{classifier head} dominates importance by an order of
magnitude ($2.17\times10^{-1}$ versus $\le 1.07\times10^{-2}$ for every other group),
with the input-side embeddings (text-word, image-patch) least important. The most
task-critical---and therefore most overwritten---parameters concentrate at the output
side of the network rather than in the input encoders.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{diag_fisher_bars.pdf}
  \caption[Per-component forgetting profile]{Per-component forgetting profile: per-group
  empirical Fisher importance (log scale) for the full multimodal model. The classifier
  head dominates by roughly an order of magnitude over every other component, while the
  input-side embeddings are least important (cf.\ Table~\ref{tab:fisher-component}).
  \textbf{Preliminary} pilot result (C4, naive sequential, three seeds).}
  \label{fig:per-component-bars}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.72\linewidth]{diag_forgetting_matrix.pdf}
  \caption[Forgetting-matrix heatmap]{Heatmap of the performance matrix $R[i,j]$ (entity-F1
  on task $j$ after training task $i$; C4, seed 42). The diagonal (just-learned) stays high
  while the sub-diagonal (retention of an earlier task after later training) collapses to
  $\sim$2--4 F1 --- forgetting is near-total within a single task step.
  \textbf{Preliminary} pilot result.}
  \label{fig:forgetting-matrix-viz}
\end{figure}

\begin{table}[!htbp]
  \centering
  \caption[Forgetting matrix under naive sequential fine-tuning]{Performance matrix
  $R[i,j]$: entity-F1 on task $j$ after training task $i$, for the full multimodal
  model (C4, seed 42) under naive sequential fine-tuning. The diagonal is strong;
  the off-diagonal collapses to $\sim$2 F1, i.e.\ near-total forgetting within one
  task step. \textbf{Preliminary} pilot result.}
  \label{tab:forgetting-matrix}
  \begin{tabular}{lccc}
    \toprule
                       & \textbf{eval FUNSD} & \textbf{eval CORD} & \textbf{eval SROIE} \\
    \midrule
    after FUNSD        & 88.0 & --   & --   \\
    after CORD         & 1.8  & 93.0 & --   \\
    after SROIE        & 1.9  & 4.1  & 80.5 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[!htbp]
  \centering
  \caption[Per-component Fisher importance]{Empirical Fisher information per
  parameter group for the full multimodal model (C4), averaged over seeds and task
  boundaries ($n{=}18$). The classifier head dominates by an order of magnitude.
  \textbf{Preliminary} pilot result.}
  \label{tab:fisher-component}
  \begin{tabular}{lr}
    \toprule
    \textbf{Component group} & \textbf{Mean Fisher} \\
    \midrule
    Classifier head             & $2.17\times10^{-1}$ \\
    Other                       & $1.07\times10^{-2}$ \\
    Text self-attention         & $4.69\times10^{-3}$ \\
    Layout 2D-position embedding & $2.98\times10^{-3}$ \\
    Feed-forward (FFN)          & $1.87\times10^{-3}$ \\
    Image patch embedding       & $1.18\times10^{-3}$ \\
    Text word embedding         & $1.99\times10^{-5}$ \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Per-Layer Breakdown}
\label{sec:results-layer}

The per-layer \gls{cka} between consecutive checkpoints
(Table~\ref{tab:cka-layer}) localises the drift. A clean \emph{monotonic depth
gradient} emerges: the text\,+\,layout embeddings and the vision patch-embed do not
move at all ($\mathrm{CKA}=1.000$), while drift rises with depth and peaks at the
last encoder layer ($0.245$) and the classifier ($0.215$) at the first task
boundary. Forgetting is thus a \emph{late-layer\,+\,head} phenomenon, not a
fusion-specific or input-encoder one. Drift is also larger at the first boundary
($0\rightarrow1$) than the second ($1\rightarrow2$), consistent with representations
stabilising as more tasks arrive. This per-layer picture and the Fisher profile of
\S\ref{sec:results-profile} agree: the output side of the network is where
task-specific knowledge concentrates and is overwritten.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.88\linewidth]{diag_cka_depth.pdf}
  \caption[Per-layer representational drift (CKA depth gradient)]{Per-layer
  representational drift (linear CKA between consecutive checkpoints) as a function of
  depth, for the two task boundaries. The input encoders do not drift ($\mathrm{CKA}=1.0$)
  while drift rises monotonically with depth and peaks at the last encoder layer and the
  classifier head (cf.\ Table~\ref{tab:cka-layer}). \textbf{Preliminary} pilot result (C4).}
  \label{fig:cka-heatmap}
\end{figure}

\begin{table}[!htbp]
  \centering
  \caption[Per-layer representational drift (CKA)]{Linear CKA between consecutive
  checkpoints ($1.0=$ no drift) for the full multimodal model (C4, default order).
  Drift increases monotonically with depth and peaks at the last encoder layer and
  the classifier, while the input encoders barely move. \textbf{Preliminary} pilot
  result.}
  \label{tab:cka-layer}
  \begin{tabular}{lcc}
    \toprule
    \textbf{Layer} & \textbf{FUNSD$\rightarrow$CORD} & \textbf{CORD$\rightarrow$SROIE} \\
    \midrule
    Text+layout embeddings  & 1.000 & 1.000 \\
    Vision patch-embed      & 1.000 & 1.000 \\
    Encoder layer 0 (early) & 0.777 & 0.989 \\
    Encoder layer 5 (mid)   & 0.344 & 0.924 \\
    Encoder layer 11 (late) & 0.245 & 0.774 \\
    Classifier head         & 0.215 & 0.708 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Stability of the Finding}
\label{sec:results-stability}

\paragraph{Cross-condition and per-component tests.}
Two Mann--Whitney U tests (Bonferroni-corrected, $\alpha=0.0167$) probe the
hypotheses of \S\ref{sec:method-diag-hypotheses}. The \emph{cross-condition} test---is
the full multimodal model's forgetting distinguishable from the unimodal/ablated
conditions?---\textbf{fails to reject} $H_0$ ($p = 0.026 / 0.028 / 0.167$ for C4 vs
C1/C2/C3). The C4-vs-C1 gap is large (BWT $\approx-81$ vs $0$) but, with $n{=}6$ and
the variance injected by the collapsed runs below, does not clear the corrected
threshold. The \emph{per-component} test, by contrast, \textbf{rejects} $H_0$: the
classifier group is the dominant forgetting locus, significantly above the
image-patch and text-word embeddings. Per the pre-registered decision rule
(\S\ref{sec:method-selection}), a head-dominated signal does not uniquely select any
of the three \emph{architectural} candidates (fusion, layout-position,
modality-routing), so the diagnosis points to the characterisation-first reading of
\S\ref{sec:method-fallback}: forgetting is severe and late-layer/head-concentrated,
but no single architectural component dominates in a way that mandates one structural
remedy.

\paragraph{Forgetting by condition.}
Table~\ref{tab:condition-metrics} reports the continual-learning metrics by
modality-ablation condition. The full model (C4) and the text\,+\,layout model (C3)
forget comparably (BWT $-81$ and $-88$), confirming that the diagnosis is not an
artefact of any one input stream.

\begin{table}[!htbp]
  \centering
  \caption[Forgetting by diagnostic condition]{Continual-learning metrics by
  modality-ablation condition (mean\,$\pm$\,std over three seeds), naive sequential
  FUNSD\,$\rightarrow$\,CORD\,$\rightarrow$\,SROIE. Text-deprived conditions (C1, C2)
  collapse to F1\,$\approx$\,0 for most seeds, inflating their variance (see
  ``modality collapse'' below). \textbf{Preliminary} pilot result.}
  \label{tab:condition-metrics}
  \begin{tabular}{llccc}
    \toprule
    \textbf{Cond.} & \textbf{Active streams} & \textbf{AA} & \textbf{BWT} & \textbf{AF} \\
    \midrule
    C1 & text only      & $0.00 \pm 0.00$  & $0.00 \pm 0.00$    & $0.00 \pm 0.00$ \\
    C2 & layout+vision  & $7.21 \pm 10.20$ & $-18.79 \pm 26.57$ & $18.79 \pm 26.57$ \\
    C3 & text+layout    & $27.72 \pm 0.44$ & $-87.62 \pm 0.62$  & $87.62 \pm 0.62$ \\
    C4 & full           & $31.63 \pm 3.51$ & $-81.26 \pm 6.41$  & $81.26 \pm 6.41$ \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Modality collapse.}
A secondary finding tempers the cross-condition test. Conditions that remove the text
stream frequently collapse during \emph{task-0} training: C1 (text-only) trained in
$0/3$ seeds and C2 (no text) in $1/3$, whereas C3 (text+layout) and C4 (full) trained
stably in $3/3$ (and $3/3$ alternate-order) seeds. The collapse is a training failure,
not forgetting---collapsed runs already score $0$ F1 immediately after task 0, while
the one stable text-deprived run shows normal continual-learning behaviour. This
indicates that the combination of text and layout drives stable optimisation and that
the \emph{text stream is load-bearing} for LayoutLMv3, an interpretable finding in its
own right; but it also means the text-deprived conditions contribute mostly zeros,
which inflates their variance and weakens the cross-condition statistic above.

\paragraph{Order dependence.}
Finally, forgetting is order-sensitive. Reversing the task order
(SROIE\,$\rightarrow$\,CORD\,$\rightarrow$\,FUNSD) measurably reduces forgetting
relative to the default order (BWT $-75.23$ vs $-81.26$; AA $34.75$ vs $31.63$):
ending on the small 7-label FUNSD task leaves more of the sequence intact. Single-order
conclusions are therefore reported with this caveat, which the pilot deliberately
captured with the alternate-order runs.

\paragraph{Synthesis.}
Figure~\ref{fig:where-forgetting} draws the three diagnostic threads together. Forgetting
concentrates in the classifier head and late encoder layers while the input encoders stay
stable; it is not a fusion-specific effect (the full model is statistically
indistinguishable from its ablations), and the text stream is load-bearing. Because the
forgetting locus is \emph{output-facing}, the diagnosis suggests --- as an open hypothesis
tested in \S\ref{sec:results-remedy} --- that remedies which protect or re-expose the head,
such as experience replay, should be the most effective intervention; a mechanism that
targets the head and late layers specifically is the proposed-method extension
(\S\ref{sec:method-fallback}).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{diag_where_forgetting.pdf}
  \caption[Where forgetting lives: synthesis]{Synthesis of the per-component diagnosis on
  the LayoutLMv3 stack: drift and parameter importance increase with depth, peaking at the
  classifier head, while the input encoders are stable. The panel states the data-supported
  finding and the diagnosis-grounded (open) hypothesis. \textbf{Preliminary} pilot result.}
  \label{fig:where-forgetting}
\end{figure}

\section{Mechanism-Targeted Remedy versus Baselines}
\label{sec:results-remedy}

This section reports the main continual-learning grid. All six core methods are
complete and filled below: the lower bound (naive), the upper bound (joint), the two
regularisation/distillation methods (EWC, LwF), and the two replay methods (ER,
DER++). The prompt/LoRA baselines and the proposed DocCL remedy were deferred (the
diagnosis triggered the characterisation-only fallback,
\S\ref{sec:method-fallback}); their rows are left as explicit em-dash placeholders.

\subsection{Main Comparison}
\label{sec:results-main-table}

Table~\ref{tab:main-comparison} and Figure~\ref{fig:aa-bars} report the average accuracy
of each method on the three scenarios. Four patterns hold across seeds. First, naive
fine-tuning is far below the joint oracle in every scenario, with the gap largest on the
domain-incremental scenario ($38.4$ vs $84.8$). Second, the two \emph{replay} methods are
the strongest non-oracle strategies by a wide margin and behave almost identically: on
DIL both ER ($88.0$) and DER++ ($87.6$) statistically match---indeed marginally
exceed---the joint upper bound ($84.8$), closing essentially the entire $46$-point
forgetting gap; on the Mixed scenario both reach the joint ceiling as well (ER $60.7$,
DER++ $61.8$ vs joint $62.8$). Third, regularisation (EWC) helps clearly on the two
scenarios with a domain shift (DIL $43.1$, the best non-replay result there) but slightly
\emph{hurts} average accuracy on fine-grained class-incremental learning (CIL-CORD
$14.3$ vs naive $18.4$), because its Fisher penalty trades plasticity for stability.
Fourth, distillation (LwF) tracks the naive lower bound throughout---in this dense
token-classification setting LwF gives essentially no benefit, because the old-class
logits it distils collapse anyway as the classifier head grows. The hardest scenario for
every method is CIL-CORD, where even the joint oracle reaches only $30.9$ AA: packing all
$60$ fine-grained CORD classes into one growing head is an intrinsically difficult
multi-class problem, independent of forgetting.

\begin{table}[!htbp]
  \centering
  \caption[Main comparison: average accuracy across scenarios]{Average entity-F1
  (AA, mean\,$\pm$\,std over three seeds) of each method on the three continual
  scenarios. Best non-oracle result per column in bold. Six core methods complete
  ($54/54$ runs) under the single-GPU configuration (RTX 2060, 3 epochs, batch 2),
  held fixed across methods. Em-dash cells: the prompt-based (L2P, DualPrompt,
  CODA-Prompt), LoRA (O-LoRA), and proposed DocCL rows are deferred
  (\S\ref{sec:results-discussion}).}
  \label{tab:main-comparison}
  % Auto-loaded from generated/table_main.tex (scripts/analyze_results.py + ingest_to_thesis.py);
  % the inline table below is a hand-kept mirror of the final numbers as a fallback.
  \IfFileExists{generated/table_main.tex}{\input{generated/table_main}}{%
  \begin{tabular}{lccc}
    \toprule
    \textbf{Method} & \textbf{CIL-CORD} & \textbf{DIL} & \textbf{Mixed} \\
    \midrule
    Naive (lower bound)   & $18.4 \pm 0.3$ & $38.4 \pm 0.8$ & $30.4 \pm 2.8$ \\
    EWC                   & $14.3 \pm 1.2$ & $43.1 \pm 3.1$ & $30.9 \pm 2.8$ \\
    LwF                   & $\mathbf{18.6 \pm 0.3}$ & $38.4 \pm 0.8$ & $32.3 \pm 0.8$ \\
    ER                    & $15.7 \pm 0.4$ & $\mathbf{88.0 \pm 0.4}$ & $60.7 \pm 0.7$ \\
    DER++                 & $17.6 \pm 0.2$ & $87.6 \pm 0.4$ & $\mathbf{61.8 \pm 0.2}$ \\
    L2P                   & -- & -- & -- \\
    DualPrompt            & -- & -- & -- \\
    CODA-Prompt           & -- & -- & -- \\
    O-LoRA                & -- & -- & -- \\
    \textbf{DocCL (ours)} & -- & -- & -- \\
    \midrule
    Joint (upper bound)   & $30.9 \pm 0.3$ & $84.8 \pm 0.7$ & $62.8 \pm 1.6$ \\
    \bottomrule
  \end{tabular}}
\end{table}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{bench_aa_bars.pdf}
  \caption[Average accuracy by method and scenario]{Average accuracy (AA) of each method
  on the three scenarios, grouped by scenario, with the naive lower bound and the joint
  upper bound for reference. Both replay methods (ER, DER++) reach the joint ceiling on
  DIL and Mixed; the prompt/LoRA/DocCL methods are deferred (Table~\ref{tab:main-comparison}).}
  \label{fig:aa-bars}
\end{figure}

\subsection{Forgetting and Plasticity Breakdown}
\label{sec:results-bwt-fwt}

Backward transfer (Table~\ref{tab:bwt-comparison}, Figure~\ref{fig:bwt-bars}) sharpens
the ranking. Naive and LwF forget almost totally on CIL-CORD (BWT $\approx-90$); EWC
roughly halves the forgetting on the two scenarios with a domain shift (DIL $-36.5$,
Mixed $-31.3$ vs naive $-71.3$ and $-66.8$) but barely helps on CIL-CORD; and the two
replay methods all but eliminate forgetting on DIL---ER reaches a slightly
\emph{positive} backward transfer ($+1.9$) and DER++ a near-zero $-2.1$, meaning later
tasks no longer overwrite earlier ones under replay. On the Mixed scenario replay again
leads (ER $-17.0$, DER++ $-32.6$ vs naive $-66.8$), and on the hardest scenario,
CIL-CORD, ER gives the single best backward transfer of any method ($-63.8$ vs naive
$-90.4$). The two replay variants differ in character: ER (pure rehearsal) is the
stronger forgetting-reducer on CIL-CORD and Mixed, whereas DER++ (rehearsal plus
logit distillation) is the more \emph{seed-stable} (standard deviations near $0.2$) and
ties ER on DIL. The method ordering for forgetting prevention is thus
naive\,$\approx$\,LwF\,$<$\,EWC\,$<$\,DER++\,$\lesssim$\,ER, with both replay methods
reaching the zero-forgetting joint oracle on the domain-incremental scenario.

\paragraph{Forward transfer.} The continual-learning loop evaluates each model only
on tasks seen so far, so the performance matrix is lower-triangular and the
forward-transfer term FWT (which needs the zero-shot $R[j-1,j]$ entries) is
\emph{not measured} and is reported as ``--''. This is a deliberate, honest gap rather
than a missing number: recovering FWT requires additionally evaluating each model on the
not-yet-seen tasks at every boundary and seeding the metric tracker with single-task
baseline performance; the change needed to enable it is documented but out of scope for
this run. The single-task baselines that FWT would subtract are nonetheless available
(e.g.\ FUNSD $b_i\approx85$), tabulated by the analysis script for future use.

\begin{table}[!htbp]
  \centering
  \caption[Backward transfer across scenarios]{Backward transfer (BWT, mean\,$\pm$\,std
  over three seeds); closer to $0$ is less forgetting. Best non-oracle per column in
  bold. Six core methods complete; em-dash cells (prompt/LoRA/DocCL) as in
  Table~\ref{tab:main-comparison}.}
  \label{tab:bwt-comparison}
  \begin{tabular}{lccc}
    \toprule
    \textbf{Method} & \textbf{CIL-CORD} & \textbf{DIL} & \textbf{Mixed} \\
    \midrule
    Naive (lower bound) & $-90.4 \pm 1.0$ & $-71.3 \pm 2.5$ & $-66.8 \pm 2.5$ \\
    EWC                 & $-77.2 \pm 2.3$ & $-36.5 \pm 2.0$ & $\mathbf{-31.3 \pm 5.9}$ \\
    LwF                 & $-90.4 \pm 0.7$ & $-72.0 \pm 1.3$ & $-65.3 \pm 1.0$ \\
    ER                  & $\mathbf{-63.8 \pm 4.4}$ & $\mathbf{+1.9 \pm 1.5}$ & $-17.0 \pm 4.3$ \\
    DER++               & $-82.8 \pm 1.7$ & $-2.1 \pm 0.8$ & $-32.6 \pm 0.1$ \\
    \midrule
    Joint (upper bound) & $0.0$ & $0.0$ & $0.0$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{bench_bwt_bars.pdf}
  \caption[Backward transfer by method and scenario]{Backward transfer (BWT) of each method,
  grouped by scenario; bars hang below zero, so shorter is less forgetting. EWC roughly
  halves forgetting where a domain shift is present, and both replay methods (ER, DER++)
  drive BWT to $\approx 0$ on DIL. Joint has BWT $=0$ by construction.}
  \label{fig:bwt-bars}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{diag_forgetting_curves.pdf}
  \caption[Forgetting trajectory under naive fine-tuning]{Per-task entity-F1 as the naive
  sequential pilot progresses (C4, seed 42). Each task is learned to a strong F1 when it is
  the current task (bold markers: 88.0 / 93.0 / 80.5) and then collapses to $\sim$2--4 F1
  once the next task is trained --- the rows of Table~\ref{tab:forgetting-matrix} read left
  to right. \textbf{Preliminary} pilot result.}
  \label{fig:forgetting-curves}
\end{figure}

\subsection{Targeting versus Uniform Treatment}
\label{sec:results-key-ablation}

This comparison---does concentrating a remedy on the dominant-forgetting component
beat treating all components uniformly?---requires the proposed DocCL mechanism, which
is \emph{deferred}. The pilot diagnosis produced a characterisation-first result
(\S\ref{sec:results-stability}, \S\ref{sec:method-fallback}): the dominant forgetting
locus is the classifier head rather than any single \emph{architectural} lever (fusion,
layout-position, modality-routing), so the cross-condition test did not select one of
the three pre-registered candidates and the targeted mechanism was not finalised within
this work. The component-targeting ablation is therefore left as future work
(\S\ref{sec:results-discussion}); the completed benchmark already establishes the
strong-baseline context (replay reaching the oracle) against which such a mechanism
would have to compete.

\section{Ablation Studies}
\label{sec:results-ablation}

The component-targeting ablation (Table~\ref{tab:ablation}) would turn the DocCL
mechanism on for each component in isolation to test whether the diagnosis predicts the
best target. It depends on the proposed mechanism and is therefore \emph{deferred to
future work} (the diagnosis triggered the characterisation-only fallback,
\S\ref{sec:results-key-ablation}); the scaffold below is retained for that future run.

\begin{table}[!htbp]
  \centering
  \caption[Component-targeting ablation (deferred)]{Effect of concentrating the
  DocCL mechanism on each component in isolation; the diagnosis would predict which
  target is best. \emph{Deferred to future work} --- depends on the proposed mechanism,
  which was not finalised after the inconclusive diagnosis
  (\S\ref{sec:results-key-ablation}).}
  \label{tab:ablation}
  % Generated by scripts/analyze_results.py + ingest_to_thesis.py; scaffold is the fallback.
  \IfFileExists{generated/table_ablation.tex}{\input{generated/table_ablation}}{%
  \begin{tabular}{lcc}
    \toprule
    \textbf{Targeted component} & \textbf{AA} & \textbf{BWT} \\
    \midrule
    Text only     & -- & -- \\
    Visual only   & -- & -- \\
    Layout only   & -- & -- \\
    Fusion only   & -- & -- \\
    Uniform (all) & -- & -- \\
    \bottomrule
  \end{tabular}}
\end{table}

\section{Computational Efficiency}
\label{sec:results-compute}

Table~\ref{tab:compute} reports the wall-clock and memory cost of the six methods,
measured on the single 6\,GB GPU (batch 2, gradient checkpointing). Every method stays
within the $5$\,GB VRAM ceiling---a hard constraint of the hardware that shaped the
whole configuration. Regularisation and distillation add a modest constant overhead:
EWC stores the diagonal Fisher and a parameter snapshot ($\sim$4.7\,GB peak), LwF a
frozen teacher copy ($\sim$3.0\,GB). The replay methods are the expensive ones: ER and
DER++ both run a second forward/backward pass over a replay batch at every step,
roughly doubling the per-run wall-clock (a CIL-CORD run takes $\sim$2.3\,h for ER and
$\sim$3--4\,h for DER++, versus $\sim$0.5\,h for naive), and DER++ additionally stores
cached output logits alongside the replay buffer. This is the practical price of
replay's accuracy: it is the strongest remedy but also the slowest. ``Run'' denotes one
full continual scenario (three to six tasks).

\begin{table}[!htbp]
  \centering
  \caption[Computational overhead]{Wall-clock and memory overhead of the six
  continual-learning methods relative to naive fine-tuning, measured on a single
  6\,GB RTX 2060 (batch 2, gradient checkpointing). ``Run'' $=$ one full continual
  scenario. The DocCL row is deferred (\S\ref{sec:results-discussion}).}
  \label{tab:compute}
  % Auto-loaded from generated/table_compute.tex; the inline table below mirrors the final numbers.
  \IfFileExists{generated/table_compute.tex}{\input{generated/table_compute}}{%
  \begin{tabular}{llll}
    \toprule
    \textbf{Method} & \textbf{Wall-clock / run} & \textbf{Extra state} & \textbf{Peak VRAM} \\
    \midrule
    Naive          & $\sim$0.5 h      & ---                          & $\sim$2.6 GB \\
    EWC            & $\sim$0.7 h      & diagonal Fisher + $\theta^\star$ snapshot & $\sim$4.7 GB \\
    LwF            & $\sim$0.7 h      & frozen teacher copy          & $\sim$3.0 GB \\
    ER             & $\sim$1.2--2.3 h & 200-doc replay buffer        & $\sim$4.0 GB \\
    DER++          & $\sim$3--4 h     & buffer + cached logits       & $\sim$4.7 GB \\
    \textbf{DocCL} & --               & ---                          & -- \\
    \bottomrule
  \end{tabular}}
\end{table}

\section{Discussion}
\label{sec:results-discussion}

\paragraph{What the diagnosis means.}
The per-component diagnosis (\S\ref{sec:results-diagnosis}) gives a clear, if
sobering, picture: under naive sequential fine-tuning LayoutLMv3 forgets
catastrophically, and the forgetting concentrates in the \emph{classifier head and the
late encoder layers}, not in the input encoders or a multimodal fusion component. The
cross-condition test could not statistically separate the full multimodal model from
its ablated variants, so the original premise that multimodal encoders forget
differently \emph{in fusion} is not supported by this pilot. This is itself
actionable: it suggests that remedies which protect or modularise the upper layers and
the classifier---of which experience replay, by directly re-exposing old-class targets
at the output, is the simplest instance---are the natural intervention, and it explains
\emph{why} replay is so effective in the benchmark below
(Figure~\ref{fig:replay-mechanism}).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{replay_mechanism.pdf}
  \caption[Why replay works]{From diagnosis to result: because forgetting is output-facing,
  replay's re-exposure of old-class targets keeps the classifier head aligned, which
  explains its empirical win (both ER and DER++ reach DIL BWT $\approx 0$). The
  diagnosis-grounded hypothesis --- that a mechanism targeting the head/late layers
  directly (DocCL) could match replay without storing data --- remains open future work.}
  \label{fig:replay-mechanism}
\end{figure}

\paragraph{What the benchmark shows.}
Across the three scenarios the ordering is unambiguous: replay $\gg$ regularisation
$\gg$ distillation. Both replay methods essentially solve the domain-incremental
scenario (ER AA $88.0$/BWT $+1.9$; DER++ $87.6$/$-2.1$, both matching the joint oracle)
and reach the joint ceiling on Mixed; EWC roughly halves forgetting wherever a domain
shift is present but not on fine-grained class-incremental learning; and LwF tracks the
naive lower bound throughout. That the two replay variants land so close together---and
that pure ER edges DER++ on the two hardest scenarios while DER++ is the more
seed-stable---suggests the benefit comes from \emph{rehearsing past examples} per se
rather than from the logit-distillation term DER++ adds on top. The dense
token-classification setting with a growing head is the likely reason distillation
(LwF) fails: the old-class logits it distils decay regardless once their classes stop
being reinforced. The scenario lens matters too---domain-incremental learning, with
distinct documents under a fixed label space, is the most tractable, whereas
fine-grained class-incremental learning over a single dataset (60 classes in one
growing head) is the hardest for every method, oracle included.

\paragraph{Threats to validity.}
Several caveats bound these conclusions. (i) The configuration is reduced relative to
the full protocol---three epochs per task (not ten) and batch size two on a single
6\,GB GPU---but it was held \emph{fixed across all six methods}, so the method
comparison is internally valid even if the absolute numbers would shift upward at ten
epochs; the small per-seed standard deviations indicate the rankings are stable. (ii)
The additional prompt/LoRA baselines and the proposed DocCL remedy with its targeting
ablation were \emph{not run}---the Week-4 diagnosis was inconclusive and triggered the
characterisation-only fallback---so the comparison covers the six core methods rather
than the full registered design space. (iii) Each scenario uses a single fixed task
order in the main grid; the pilot showed order materially affects forgetting
($\sim$6 BWT points), so single-order numbers carry that caveat. (iv) Forward transfer
is not measured (\S\ref{sec:results-bwt-fwt}); it is reported as unavailable rather than
estimated. (v) The pilot's text-deprived conditions collapsed, which limited the
statistical power of the cross-condition test. (vi) All results are on the single
primary backbone (LayoutLMv3); whether the late-layer/head concentration and the
replay-dominant ordering generalise to LiLT and BROS is a planned secondary-backbone
check. None of these undercut the chapter's two robust findings---forgetting is a
late-layer/head phenomenon, and replay (ER/DER++) is the strongest remedy, reaching the
joint oracle on domain-incremental learning---but they delimit how far the exact numbers
should be generalised.
