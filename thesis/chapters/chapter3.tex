% =====================================================================
\chapter{Methodology}
\label{ch:methodology}
% =====================================================================

This chapter presents the two methodological contributions of the thesis. First,
a \emph{per-component forgetting diagnostic} for tri-modal document encoders
(\S\ref{sec:method-diagnostic}). Second, a \emph{mechanism-targeted continual-learning
method}, DocCL, whose design is derived from the diagnosis rather than chosen in
advance (\S\ref{sec:method-remedy}). The ordering is deliberate: the diagnosis is
the primary contribution and constrains the remedy.

\section{Problem Formulation}
\label{sec:method-formulation}

We study continual \gls{kie} as token-level sequence labelling. It is useful to
separate three layers of ``input/output'' that are easily conflated: at the
\emph{user} level the input is a document scan and the output is extracted
information; at the \emph{machine-learning} level the input is a stream of
datasets $(\mathcal{D}_1,\mathcal{D}_2,\dots)$ and the output is the trained
parameters; at the \emph{technical} level---the one we formalise---the input is a
document tuple and the output is a label sequence. (All symbols introduced here
are collected in the List of Notations.)

A document is a triple $D = (W, B, I)$ where $W = (w_1,\dots,w_N)$ is a sequence of
$N$ \gls{ocr} tokens, $B = (b_1,\dots,b_N)$ with $b_i \in [0,1000]^4$ are
normalised bounding boxes, and $I \in \mathbb{R}^{3\times224\times224}$ is the page
image. The model $f_\theta$ produces \gls{bio} labels $Y = (y_1,\dots,y_N)$ with
$y_i \in \mathcal{L}_{\text{task}}$, trained by token-level cross-entropy $\ell$
with ignored positions masked out. Performance is measured by entity-level F1 with
\gls{bio}-aware span matching.

In the continual setting the model sees a sequence of tasks $\mathcal{T}_1, \dots,
\mathcal{T}_T$, each with its own data and (in \gls{cil}) its own growing label
space. Let $\theta$ denote the encoder parameters and $\theta^{(t)}$ the parameters
after training task $t$; the learner may access only $\mathcal{D}_t$ and (if any) a
memory $M$ at task $t$. The continual objective minimises the summed risk over all
tasks,
\[
  \theta_T = \arg\min_{\theta}\ \sum_{t=1}^{T}
    \mathbb{E}_{(x,y)\sim\mathcal{D}_t}\big[\ell(f_\theta(x), y)\big]
  \quad \text{s.t.}\quad |M| \le M_{\max},\ \ \lVert\Delta\theta\rVert \le P,
\]
under a bounded memory $M$ (at most $M_{\max}$ exemplars) and a parameter budget
$P$ (both relevant because the document setting favours memory-light,
privacy-preserving methods). The object of
study is how performance on earlier tasks degrades as later tasks are learned,
and---uniquely here---how that degradation is distributed across the
\emph{components} of $\theta$.

\subsection{Defining a Component}
\label{sec:method-components}

We partition the encoder parameters into groups that are \emph{genuinely
separable} in the backbone. A caveat governs this partition and is central to the
honesty of the analysis: LayoutLMv3 is a \emph{single-stream} encoder. Only the
\emph{input} embeddings are modality-specific; from the first encoder layer
onward, text and visual patch tokens are concatenated and share the \emph{same}
query/key/value projections. There is therefore no identifiable
``text-attention'' versus ``visual-attention'' versus ``cross-modal fusion''
\emph{parameter} group---those projections are one shared set. We do not pretend
otherwise. We group instead by what the architecture actually separates:

\begin{itemize}
  \item \textbf{Text word embeddings} --- the token embedding table
        (\texttt{text\_word\_embed}).
  \item \textbf{Layout embeddings} --- the 2D position embeddings encoding
        bounding-box geometry (\texttt{layout\_2d\_pos\_embed}).
  \item \textbf{Visual patch embedding} --- the image-patch projection
        (\texttt{image\_patch\_embed}).
  \item \textbf{Shared self-attention} --- the (single-stream) query/key/value
        projections (\texttt{attn\_qkv}) and the attention output projection
        (\texttt{attn\_out}), reported separately rather than folded into the
        feed-forward block.
  \item \textbf{Feed-forward and normalisation} --- the position-wise
        feed-forward blocks (\texttt{ffn}) and layer norms (\texttt{layernorm}).
  \item \textbf{Task head} --- the token-classification layer
        (\texttt{classifier}), tracked separately as it is expanded for new
        labels.
\end{itemize}

These groups are exhaustive and every one is populated; a residual catch-all is
verified empty for the stock backbone. The modalities' \emph{behavioural}
contributions are recovered separately, by the input-masking conditions of
\S\ref{sec:method-diag-ablation}, and forgetting is additionally localised by
\emph{depth} (input / early / mid / late encoder layers / head), the axis along
which a single-stream encoder is most naturally heterogeneous. For CKA tracking we
probe six depth-matched points (embeddings, patch embedding, early/mid/late
encoder layers, and the classifier) so the gradient can be compared across
backbones, including the unimodal BERT baseline.

\section{A Per-Component Forgetting Diagnostic}
\label{sec:method-diagnostic}

\subsection{Motivation}
\label{sec:method-diag-motivation}

If forgetting concentrates in a particular component, a continual-learning method
should spend its stability budget there; if it is uniform, uniform methods are
justified. Either way the answer is informative. To obtain it we need (i) a way to
isolate each modality's contribution without confounds, and (ii) metrics that
attribute forgetting to components.

\subsection{Controlled Modality Ablation}
\label{sec:method-diag-ablation}

We use two complementary controls. First, to isolate the modalities
\emph{behaviourally} without confounding architecture, we hold the architecture
fixed and mask the input streams of a \emph{single} LayoutLMv3, defining four
apples-to-apples conditions C1--C4 (Table~\ref{tab:diagnostic-conditions}): text
only (C1), image+layout (C2), text+layout (C3), and the full tri-modal input (C4).
Because only the inputs differ, any difference in forgetting is attributable to the
modalities in play rather than to the network. Critically, the text-only condition
(C1) keeps the real text tokens and zeroes \emph{only} the image and layout
streams---it is a genuine text-only LayoutLMv3, not a null input.

Second, because a same-architecture mask cannot tell us whether a multimodal
encoder forgets \emph{differently} from a unimodal one, we add an \emph{external}
unimodal baseline Cb: a true BERT-base text encoder, tokenised with its own
WordPiece vocabulary on the same documents and run through the same instrument
(per-token CKA, per-group importance, displacement, forgetting metrics). The
within-architecture conditions answer ``which stream/depth forgets''; the Cb
contrast answers ``is this multimodal-specific?''.

\begin{table}[!htbp]
  \centering
  \caption[The diagnostic conditions]{The diagnostic conditions. C1--C4 are the
  \emph{same} LayoutLMv3 architecture under different input-stream masks (only the
  active inputs differ); Cb is an external true-unimodal baseline. C1 keeps real
  text (image and layout zeroed)---it is not a null-input condition.}
  \label{tab:diagnostic-conditions}
  \begin{tabular}{cll}
    \toprule
    \textbf{ID} & \textbf{Model / active inputs} & \textbf{Purpose} \\
    \midrule
    Cb & BERT-base, text only          & external true-unimodal contrast \\
    C1 & LayoutLMv3, text only         & real text-only (image + layout zeroed) \\
    C2 & LayoutLMv3, image + layout    & isolate visual+layout-only forgetting \\
    C3 & LayoutLMv3, text + layout     & isolate text+layout-only forgetting \\
    C4 & LayoutLMv3, full tri-modal    & main subject of analysis \\
    \bottomrule
  \end{tabular}
\end{table}

Figure~\ref{fig:diagnostic-conditions} depicts the LayoutLMv3 conditions as the same
network with different input pathways switched on or off.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{diagnostic_conditions.pdf}
  \caption[The diagnostic conditions as active and zeroed pathways]{The LayoutLMv3
  conditions C1--C4 realised on a single fixed architecture. Each panel shows the
  same LayoutLMv3 with a different subset of input pathways active (text, image,
  layout) and the remainder zeroed at the input: real text only (C1), image and
  layout (C2), text and layout (C3), and the full tri-modal input (C4). The external
  unimodal baseline Cb (BERT-base) is a separate model, not a mask. Because only the
  inputs differ across C1--C4, any difference in forgetting is attributable to the
  modalities in play rather than to the network.}
  \label{fig:diagnostic-conditions}
\end{figure}

\subsection{Forgetting Decomposition Metrics}
\label{sec:method-diag-metrics}

We measure forgetting with three complementary instruments, following the
multiple-instrument principle motivated in \S\ref{sec:bg-superficial-essential}.

\paragraph{Representational drift (per-token linear CKA).} For each tracked layer
we compute the linear CKA~\citep{kornblith2019similarity} between activations of
the model after task $t-1$ and after task $t$ on a fixed probe set. Given two
centred activation matrices $X \in \mathbb{R}^{n\times p}$ and $Y \in
\mathbb{R}^{n\times q}$,
\[
  \mathrm{CKA}(X,Y) = \frac{\lVert Y^\top X \rVert_F^2}
  {\lVert X^\top X \rVert_F \, \lVert Y^\top Y \rVert_F}.
\]
A drop of CKA below $1$ at a layer marks it as a drift hotspot. Two design choices
matter for validity. First, token classification lives in the \emph{per-token}
representation, so we compute CKA over \emph{individual valid tokens} (the
classifier-relevant first-subword positions), not over a document-mean vector that
would average $\sim$500 token vectors per page and could read ``no drift'' while
the token geometry has reorganised. Second, the probe collects $n\ge 500$ distinct
tokens (well above the hidden width $p=768$), so the estimate is not in the
high-variance $n<p$ regime; activations are captured \emph{under the condition's
modality mask} so each condition is probed on the inputs it was trained on.

\paragraph{Where forgetting lives: importance vs.\ displacement.} We separate two
distinct quantities that are easily conflated. The empirical Fisher
\emph{importance} of a group $g$ on a task's own data,
\[
  F_g = \frac{1}{|\mathcal{D}|}\sum_{(x,y)\in\mathcal{D}}
        \sum_{\phi\in g}\Big(\frac{\partial \log p_\theta(y\mid x)}{\partial \phi}\Big)^2 ,
\]
measures how \emph{sensitive} the loss is to $g$ \emph{for that task}. It is
\emph{not} a forgetting signal: the classifier head carries the largest such
gradients for \emph{any} token classifier (BERT included) by the geometry of the
cross-entropy output layer, regardless of whether those parameters were
\emph{moved}. To localise \emph{forgetting} we use the old-task-Fisher-weighted
\emph{displacement} between the checkpoint before and after a task,
\[
  D_g = \sum_{\phi\in g} F^{(t-1)}_\phi\,
        \big(\theta^{(t)}_\phi - \theta^{(t-1)}_\phi\big)^2 ,
\]
which is large precisely where parameters that mattered for the \emph{previous}
task ($F^{(t-1)}$) have been overwritten. We report $D_g$ per component group and
per depth bucket (input/early/mid/late/head) as the primary forgetting localiser,
with Fisher \emph{importance} reported alongside but never labelled forgetting.
Fisher is computed under each condition's modality mask.

\paragraph{Forgetting matrix.} We record the standard accuracy matrix
$R[i,j]$, the entity-F1 on task $j$ after training task $i$, from which we derive
average accuracy, backward transfer, and average forgetting
(\S\ref{sec:eval-metrics}). Its structure is annotated in
Figure~\ref{fig:forgetting-matrix}: the diagonal holds just-learned accuracy, the
lower triangle holds retained accuracy on earlier tasks (the source of backward
transfer and forgetting), and the upper triangle---unused here, as tasks arrive
sequentially---would hold zero-shot forward transfer. This is the conventional,
component-agnostic view that the per-component metrics refine.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.6\linewidth]{forgetting_matrix_schematic.pdf}
  \caption[Annotated structure of the forgetting matrix]{Schematic structure of the
  accuracy matrix $R[i,j]$ (entity-F1 on task $j$ after training task $i$); symbolic
  labels only, with no measured values. The diagonal $R[i,i]$ records just-learned
  accuracy; the lower triangle $R[i,j]$ for $j<i$ records retained accuracy on
  earlier tasks, from which average accuracy, backward transfer, and average
  forgetting are derived; the upper triangle is unevaluated under sequential
  arrival.}
  \label{fig:forgetting-matrix}
\end{figure}

\subsection{Diagnostic Protocol}
\label{sec:method-diag-protocol}

For each condition C1--C4 and each seed we train naively and sequentially on
FUNSD\,$\rightarrow$\,CORD\,$\rightarrow$\,SROIE with no continual-learning
strategy, expanding the classifier head at each task. At every task boundary we
capture per-layer CKA, per-group Fisher, and the forgetting matrix.
Algorithm~\ref{alg:forgetting-decomposition} summarises the procedure and
Figure~\ref{fig:diagnostic-pipeline} shows it as a data-flow diagram.

\begin{algorithm}[t]
  \caption{Per-component forgetting decomposition}
  \label{alg:forgetting-decomposition}
  \begin{algorithmic}[1]
    \Require condition $c \in \{C1,C2,C3,C4\}$, task sequence $\mathcal{T}_{1:T}$,
             probe set $\mathcal{P}$, layer set $\mathcal{G}_{\text{CKA}}$,
             parameter groups $\mathcal{G}_{\text{F}}$
    \State initialise $\theta^{(0)}$ from the pre-trained backbone
    \For{$t = 1$ \textbf{to} $T$}
      \State expand classifier head to $\mathcal{L}_{\mathcal{T}_t}$
      \State $A_{t-1} \gets \textsc{Activations}(\theta^{(t-1)}, \mathcal{P})$
      \State $\theta^{(t)} \gets \textsc{FineTune}(\theta^{(t-1)}, \mathcal{T}_t)$
        \Comment{naive, modality mask $c$}
      \State $A_{t} \gets \textsc{Activations}(\theta^{(t)}, \mathcal{P})$
      \ForAll{$\ell \in \mathcal{G}_{\text{CKA}}$}
        \State $\mathrm{CKA}_{t,\ell} \gets \textsc{LinearCKA}(A_{t-1}[\ell], A_t[\ell])$
      \EndFor
      \ForAll{$g \in \mathcal{G}_{\text{F}}$}
        \State $F_{t,g} \gets \textsc{Fisher}(\theta^{(t)}, g, \mathcal{T}_t)$
      \EndFor
      \ForAll{$j \le t$}
        \State $R[t,j] \gets \textsc{EvalF1}(\theta^{(t)}, \mathcal{T}_j)$
      \EndFor
    \EndFor
    \State \Return $\{\mathrm{CKA}_{t,\ell}\}, \{F_{t,g}\}, R$
  \end{algorithmic}
\end{algorithm}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{diagnostic_pipeline.pdf}
  \caption[The per-component diagnostic pipeline]{Data flow of the per-component
  diagnostic. The model is fine-tuned sequentially on
  FUNSD\,$\rightarrow$\,CORD\,$\rightarrow$\,SROIE under a fixed modality mask, and
  at each task boundary three instruments are captured: per-layer linear CKA between
  the pre- and post-task checkpoints on a fixed probe set, per-group empirical
  Fisher importance, and a row of the accuracy matrix $R[i,j]$ over all seen tasks.
  The procedure is repeated for every condition C1--C4 and every seed.}
  \label{fig:diagnostic-pipeline}
\end{figure}

\subsection{Hypotheses and Decision Rule}
\label{sec:method-diag-hypotheses}

We test a null hypothesis $H_0$ that forgetting is uniform across the network
against $H_1$ that it concentrates along depth (in particular at the classifier
head and late encoder layers). The test uses the per-group displacement $D_g$ and
per-depth displacement of \S\ref{sec:method-diag-metrics}, aggregated \emph{per
seed first} (the per-seed statistic is the mean over that seed's boundaries) so the
unit of analysis is the seed. For the dominant (largest mean) bucket we compare its
seed-level displacements against the pooled remaining buckets with a one-sided
Mann--Whitney U test, rejecting $H_0$ for a bucket when $p_g < 0.05/(K-1)$ for $K$
buckets.

\paragraph{Statistical power (a hard constraint on the design).} A rank test on
small samples has a \emph{minimum achievable} two-sided $p$-value of
$2/\binom{n_1+n_2}{n_2}$ even under perfect separation. With three seeds per group
this floor is $2/\binom{6}{3}=0.10$, far above any Bonferroni threshold: such a
test \emph{cannot} reject $H_0$ regardless of the data, so a ``failure to reject''
would be an artefact of sample size, not evidence. We therefore run \emph{at least
five seeds} per condition (and pool the alternate task order), which lowers the
floor for the cross-condition contrast to $2/\binom{10}{5}=0.0079$, below the
corrected threshold. Every reported non-rejection is accompanied by its power
floor, and any comparison whose floor exceeds $\alpha$ is labelled
\emph{inconclusive by construction} rather than evidence for $H_0$.

\paragraph{Location, not just magnitude.} Asking whether a multimodal encoder
forgets \emph{differently} is a question about the \emph{distribution} of
forgetting across components, which a scalar test on total backward-transfer
magnitude cannot answer. We therefore add a distribution-targeting test: for each
condition we form the per-depth displacement \emph{profile} (an $L_1$-normalised
vector over buckets) and compare the profile of C4 against each unimodal reference
(C1 and the external Cb) with a permutation test on the cosine distance between
mean profiles. This directly tests ``do they forget in a different \emph{place}?''.

The diagnostic outcome maps to the remedy through the measurable branches of
Table~\ref{tab:subhypotheses} and Figure~\ref{fig:selection-tree}. We deliberately
restrict the decision to signals the instrument can actually produce: because
LayoutLMv3 is single-stream, ``fusion-dominant'' or ``visual-attention-dominant''
branches are \emph{not realisable} and are excluded; the admissible outcomes are a
head/late-layer concentration, a uniform-across-depth pattern, or no concentration.

\begin{table}[!htbp]
  \centering
  \caption[Diagnostic outcomes and the remedy each selects]{Measurable diagnostic
  outcomes and the remedy each selects. Only outcomes the single-stream instrument
  can realise are admitted; the decision is a function of the pilot
  (\S\ref{sec:method-selection}).}
  \label{tab:subhypotheses}
  \begin{tabularx}{\textwidth}{l L l}
    \toprule
    \textbf{Pattern} & \textbf{Signature} & \textbf{Triggers} \\
    \midrule
    $H_a$ head/late-dominant & Displacement concentrates at the head and late layers & DocCL: depth/head-targeted (\S\ref{sec:method-selection}) \\
    $H_b$ uniform-across-depth & Displacement broadly even across depth buckets & Uniform consolidation \\
    $H_0$ no concentration & No bucket separates after correction (with adequate power) & Characterisation-only (\S\ref{sec:method-fallback}) \\
    \bottomrule
  \end{tabularx}
\end{table}

The mapping from diagnostic outcome to remedy is made explicit in
Figure~\ref{fig:selection-tree}: the decision is a function of the pilot, so the
mechanism is not committed to in advance, and the no-concentration branch leads to
the characterisation-only fallback rather than to a method.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{method_selection_tree.pdf}
  \caption[Decision tree mapping diagnostic outcomes to the remedy]{The decision
  rule as a tree over \emph{measurable} outcomes. A head/late-layer concentration
  ($H_a$) selects the depth/head-targeted DocCL; a uniform-across-depth pattern
  ($H_b$) selects uniform consolidation; no significant concentration (at adequate
  power, $H_0$) selects the characterisation-only fallback. Unrealisable branches
  (fusion- or visual-attention-dominant) are excluded because a single-stream
  encoder cannot produce those signals. \emph{Figure to be regenerated from
  \texttt{figures\_src/drawio/} to match these branches.}}
  \label{fig:selection-tree}
\end{figure}

\section{Mechanism-Targeted Continual Learning (DocCL)}
\label{sec:method-remedy}

\subsection{Why Component-Agnostic Continual Learning Is Insufficient}
\label{sec:method-remedy-motivation}

Generic continual-learning methods treat the encoder uniformly. EWC penalises all
important parameters with a single Fisher-weighted term regardless of which
component they belong to; LW-AFT freezes layers by a fixed heuristic. If
forgetting concentrates in one component, both spend stability budget where it is
not needed and may under-protect where it is. The hypothesis of DocCL is that
\emph{concentrating} the continual-learning mechanism---be it regularisation
strength, freezing, or replay weighting---on the dominant-forgetting component
improves the stability--plasticity trade-off at negligible extra cost.

\subsection{Design Space: Mapping CL Families to LayoutLMv3}
\label{sec:method-design-space}

The candidates are constructed from three continual-learning families
(\S\ref{sec:bg-cl-families}), each adapted to the tri-modal structure of
LayoutLMv3. What is novel here is not the families themselves but \emph{where} they
attach to a document encoder, since no published method exploits the layout stream.

\paragraph{Where LoRA injects.} Low-rank adapters~\citep{hu2022lora} can be placed
at four sites in LayoutLMv3: the text self-attention Q/K/V projections (the
standard NLP recipe), the image-patch projection and visual self-attention, the 2D
position (layout) embeddings, and the cross-modal fusion projection. The fusion
projection offers the most leverage per parameter because it is the only site where
the three streams interact; the layout site is the least explored and is plausibly
where forms-versus-receipts shift concentrates gradient. O-LoRA~\citep{wang2023olora}
keeps per-task subspaces globally orthogonal, which exhausts rank as tasks
accumulate; allocating \emph{separate banks per component} relaxes this to a
per-bank constraint.

\paragraph{Where prompts attach.} Prompt-pool methods such as
L2P~\citep{wang2022l2p} select learnable tokens by querying a frozen backbone with
a CLS feature. For a tri-modal encoder this raises two design questions: which
stream(s) the prompts prepend to (text, visual, fusion), and whether the
query/key mechanism should be made \emph{layout-aware} rather than CLS-only. Both
are exploited by the candidates below.

\paragraph{Where regularisation applies.} Rather than a single Fisher-weighted
penalty over all parameters (EWC~\citep{kirkpatrick2017ewc}), the importance weight
can be made \emph{component-specific}, concentrating protection on whichever group
the diagnosis flags as fastest-drifting.

\subsection{Candidate Designs}
\label{sec:method-candidates}

Because the method must be derived from the diagnosis, candidate designs were
sketched in advance as a safety net for the measure-first approach. We retain the
three original sketches below as design history, with an important correction. Two
of them---Candidate~A (which assumes \emph{fusion} layers forget most) and
Candidate~C (which assumes per-modality, visual-versus-text patterns)---presuppose
signals that a \emph{single-stream} encoder cannot produce: as
\S\ref{sec:method-components} explains, LayoutLMv3 has no separable fusion or
visual-attention parameter group, so those branches are not realisable by the
instrument. The corrected, adequately powered decision rule
(\S\ref{sec:method-diag-hypotheses}) admits only the depth-localised outcomes, and
the diagnosis selects the depth/head-targeted DocCL of
\S\ref{sec:method-selected}---which is most closely related to Candidate~B
(selective, group-weighted EWC), generalised from layout-position protection to
depth/head protection. The sketches A and C are reserved as directions for the
NeurIPS extension on a two-stream backbone where the streams physically exist.

\subsubsection{Candidate A: Layout-Aware Prompts with Hierarchical LoRA (LAPP + H-LoRA)}
\label{sec:method-cand-a}
\emph{Triggered if the diagnosis shows fusion layers forget most and
layout-sensitivity matters.} This candidate combines two components.

\noindent\emph{LAPP (Layout-Aware Prompt Pool).} L2P's CLS-only query is replaced
by a query that concatenates the CLS embedding with a projected layout signature,
\[
  q(x) = \big[\, h_{\mathrm{CLS}} \;;\; W_{\text{layout}}\,\phi(\text{boxes}) \,\big].
\]
The descriptor $\phi(\text{boxes})$ is made precise as a normalised spatial
histogram. Each box $b_i$ contributes its centre
$(c_x^{(i)}, c_y^{(i)}) = \big(\tfrac{b_{i,1}+b_{i,3}}{2},\, \tfrac{b_{i,2}+b_{i,4}}{2}\big)$,
rescaled from $[0,1000]$ to $[0,1]^2$. The unit square is partitioned into a
$4\times4$ grid of $16$ equal cells, and each centre is assigned to the single cell
$\mathrm{cell}(i) = \big(\lfloor 4\,c_x^{(i)}\rfloor,\, \lfloor 4\,c_y^{(i)}\rfloor\big)$
(clamped to the boundary). Counting centres per cell and $\ell_1$-normalising gives
\[
  \phi(\text{boxes})_k = \frac{1}{N}\sum_{i=1}^{N}
    \mathbf{1}\!\big[\mathrm{cell}(i) = k\big]
  \in \mathbb{R}^{16},
  \qquad \sum_{k=1}^{16}\phi(\text{boxes})_k = 1,
\]
so $\phi(\text{boxes})$ is the layout-only, text-invariant geometry of the page (the
\texttt{get\_\allowbreak layout\_\allowbreak signature} descriptor of \S\ref{sec:impl-instrumentation});
$W_{\text{layout}}\colon\mathbb{R}^{16}\!\to\!\mathbb{R}^{768}$ is trainable. Given a
prompt pool $\{(k_i, P_i)\}_{i=1}^{M}$ of key--prompt pairs, the prepended prompts
are those whose keys are most similar to the layout-aware query under cosine
similarity,
\[
  K_x = \operatorname*{top\text{-}K}_{i}\, \cos\!\big(q(x),\, k_i\big),
\]
and the pool keys $k_i$ are enriched the same way, so that documents are routed to
prompts by spatial layout, not text alone.

\noindent\emph{H-LoRA (Hierarchical LoRA).} Three rank-$r$ LoRA banks
($r{=}8$) are allocated by component
$b\in\{\text{text},\text{visual},\text{fusion}\}$: Bank~1 on the text Q/K/V
projections, Bank~2 on the patch projection and visual self-attention, and Bank~3 on
the cross-modal fusion projection---the key bank when fusion dominates.
Orthogonality is enforced \emph{per bank} rather than globally: writing the new-task
down-projection of bank $b$ as $A_t^{(b)}$ and stacking the columns of all
earlier-task factors of the same bank as $A_{<t}^{(b)}$, the new factor is
constrained to lie orthogonally to them \emph{within that bank},
\[
  \big(A_t^{(b)}\big)^{\!\top} A_{<t}^{(b)} = 0
  \qquad \text{for each } b\in\{\text{text},\text{visual},\text{fusion}\}.
\]
This is a deliberate relaxation of O-LoRA's single global orthogonality
constraint~\citep{wang2023olora}: confining orthogonality to each component's own
bank delays the rank exhaustion that a global constraint suffers once enough tasks
accumulate, while still suppressing cross-task interference where it matters most. A
task may activate only a subset of banks, compatible with a
D-MoLE-style~\citep{ge2025dmole} layer-wise allocation. The per-task parameter cost
is small. A rank-$r$ adapter on a layer of width $d$ adds $2rd$ parameters (a
$d\times r$ down-projection and an $r\times d$ up-projection), so across $L_b$
adapted layers in bank $b$ the per-task cost is
\[
  \#\text{params}_{\text{H-LoRA}} \approx \sum_{b}\, 2\,r\,d\,L_b ,
\]
which at $r{=}8$, $d{=}768$ is on the order of $10^5$ parameters per task against the
$\sim$133M frozen backbone---roughly a tenth of a percent.
Figure~\ref{fig:candidate-a} sketches the two components together.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{candidate_a_lapp_hlora.pdf}
  \caption[Candidate~A: layout-aware prompts with hierarchical LoRA]{Candidate~A
  (LAPP + H-LoRA). \emph{Left}: the layout-aware query concatenates the CLS
  embedding with a projected $4\times4$ grid histogram of bounding-box centres and
  selects prompts from the pool by cosine top-$K$, so routing depends on page
  geometry rather than text alone. \emph{Right}: three component-wise low-rank banks
  (text, visual, fusion) with orthogonality enforced within each bank rather than
  globally; the fusion bank is the key one when fusion forgets most. The schematic is
  illustrative and does not assert that this candidate is selected.}
  \label{fig:candidate-a}
\end{figure}

\subsubsection{Candidate B: Layout-Protected Regularisation (L-EWC)}
\label{sec:method-cand-b}
\emph{Triggered if forgetting is broadly uniform but the 2D position embeddings
drift fastest.} A selective EWC variant applies a high importance weight to the
layout (2D position) embeddings and a low weight elsewhere,
\[
  \mathcal{L} = \mathcal{L}_{\mathrm{CE}}
    + \lambda_{\text{layout}}\, F_{\text{layout}}\,(\theta_{\text{layout}} - \theta^{*}_{\text{layout}})^2
    + \lambda_{\text{other}}\, F_{\text{other}}\,(\theta_{\text{other}} - \theta^{*}_{\text{other}})^2,
\]
with $\lambda_{\text{layout}} \gg \lambda_{\text{other}}$ and $F$ the per-group
Fisher of \S\ref{sec:method-diag-metrics}. An optional layout-embedding
distillation term from the previous-task model can be added. The method is cheap,
theoretically motivated, and adds no inference-time parameters.

\subsubsection{Candidate C: Modality-Routed Prompts}
\label{sec:method-cand-c}
\emph{Triggered if the forgetting pattern is scenario-dependent or per-modality.}
Three prompt sub-pools $P_{\text{text}}, P_{\text{visual}}, P_{\text{layout}}$ are
maintained, and a router activates one or more from input features,
\[
  r(x) = \mathrm{softmax}\!\big(\mathrm{MLP}([\,h_{\mathrm{CLS}};\,
  \phi(\text{boxes});\, \bar{v}\,])\big),
\]
where $\bar{v}$ is a pooled visual feature; the selected sub-pool is prepended to
its corresponding stream. This combines L2P's pool concept with explicit modality
routing and naturally fits domain-incremental shifts (where S-Prompt-style
per-domain prompts excel~\citep{wang2022sprompt}). Candidates~B and~C are contrasted
in Figure~\ref{fig:candidate-bc}.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{candidate_b_c.pdf}
  \caption[Candidates~B and~C: layout-protected regularisation and modality-routed
  prompts]{The two alternative candidates. \emph{Left} (Candidate~B, L-EWC): a
  selective elastic-weight penalty places a large importance weight on the 2D
  position (layout) embeddings and a small weight elsewhere, concentrating stability
  on the fastest-drifting parameters with no inference-time cost. \emph{Right}
  (Candidate~C, modality-routed prompts): a router maps input features to one or more
  of three per-modality prompt sub-pools, each prepended to its own stream. Both are
  shown as alternatives; the diagnosis, not this figure, decides which (if any) is
  selected.}
  \label{fig:candidate-bc}
\end{figure}

\subsection{Testable Component Hypotheses}
\label{sec:method-component-hypotheses}

Two method-level hypotheses are pre-registered and map one-to-one to the
component-targeting ablation in Chapter~\ref{ch:results}:

\begin{itemize}
  \item \textbf{H\textsubscript{target}.} Concentrating the stability budget on the
        diagnosed locus (the classifier head and late encoder layers) outperforms
        spreading the same budget uniformly across depth (the \emph{uniform}
        ablation) by $\ge 2$ entity-F1 on $\ge 2/3$ scenarios.
  \item \textbf{H\textsubscript{head}.} Head-only protection recovers most of the
        gain of the full method; adding late-layer protection and head replay adds
        a further measurable improvement (the \emph{head-only} vs.\ \emph{late-only}
        vs.\ \emph{full} ablation).
\end{itemize}

A falsified hypothesis remains informative and is reported as such.

\subsection{Selection Criterion}
\label{sec:method-selection}

The remedy follows directly from the measurable diagnostic outcome
(Table~\ref{tab:subhypotheses}): a head/late-layer concentration selects the
depth/head-targeted DocCL of \S\ref{sec:method-selected}; a uniform-across-depth
pattern selects uniform consolidation; no concentration (at adequate power) selects
the characterisation-only fallback (\S\ref{sec:method-fallback}). Beyond alignment
with the diagnosis, the method must be implementable within the compute budget,
defensible under reviewer scrutiny, and reuse the existing EWC/replay/distillation
infrastructure.

\subsection{The Selected Method: DocCL}
\label{sec:method-selected}

The diagnosis localises forgetting in the \emph{classifier head and late encoder
layers}, with the input encoders stable (Chapter~\ref{ch:results}). DocCL therefore
spends its stability budget there, combining three mutually-reinforcing, each
individually-ablatable mechanisms:

\begin{enumerate}
  \item \textbf{Depth-scaled elastic-weight consolidation.} A Fisher-weighted
        quadratic anchor to the previous task whose per-bucket weight scales with
        where forgetting lives---high on the head and late layers, near zero on the
        stable input encoders---using the depth grouping of
        \S\ref{sec:method-components}.
  \item \textbf{Output distillation.} A temperature-softened KL term matching the
        previous task's output distribution from a frozen teacher, which protects
        the output-facing (head and late) representations functionally.
  \item \textbf{Head re-exposure replay.} A small reservoir buffer---replay being
        the strongest baseline in the diagnosis---keeps the head grounded in earlier
        tasks.
\end{enumerate}

The total objective is $\mathcal{L} = \mathcal{L}_{\mathrm{CE}}
+ \tfrac{\lambda}{2}\sum_g w_g\, \mathcal{L}^{\mathrm{EWC}}_g
+ \alpha\,\mathcal{L}_{\mathrm{KD}} + \mathcal{L}_{\mathrm{replay}}$,
with depth weights $w_g$ concentrated on head/late. A \texttt{target\_depth}
control (\emph{full}, \emph{head-only}, \emph{late-only}, \emph{uniform}) drives the
component-targeting ablation that tests H\textsubscript{target} and
H\textsubscript{head}. The mechanism reuses the EWC Fisher machinery, the LwF-style
teacher, and the shared reservoir buffer, so it adds no fragile new infrastructure.

\subsection{Fallback: Characterisation-Only}
\label{sec:method-fallback}

If forgetting does not concentrate ($H_0$ not rejected \emph{at adequate power}),
the diagnostic of \S\ref{sec:method-diagnostic} and the FCS-CL benchmark become the
headline contributions, and the thesis is framed as a measurement-and-benchmark
study. The value of the diagnosis is independent of the remedy's success.

\section{Summary}
\label{sec:method-summary}

We have defined the genuinely separable components of a single-stream tri-modal
document encoder, a controlled modality-ablation protocol with an external unimodal
contrast, and complementary metrics---per-token CKA, per-group importance, an
old-task-Fisher-weighted displacement (the forgetting localiser), and the
forgetting matrix---that together localise forgetting by depth, stream, and head.
The remedy, DocCL, concentrates its stability budget on the diagnosed head/late
locus; its form follows from the diagnosis under a pre-registered, adequately
powered decision rule. The next chapter describes how this methodology is realised
in software.
