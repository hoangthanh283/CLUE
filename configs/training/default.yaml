batch_size: 8
num_workers: 4
gradient_accumulation_steps: 1
fp16: false
# Recompute activations in backward to cut training memory. Turn on for limited-VRAM
# GPUs so full fine-tuning fits at a small batch size.
gradient_checkpointing: false
